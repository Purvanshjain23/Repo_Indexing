// ?------------------------------------------------------------------------------------------------
// ?Synon action diagram for POTFXFR
// ?Date: 14.08.2025 Time: 03:38:58
// ?------------------------------------------------------------------------------------------------

//?Execute user function

//?Combine companies if par.company is -0-
EXECUTE FUNCTION(Rtv Past AvgDys Pay   RT) TYPE(RTVOBJ) FILE(PDLDCPP)           AC1658492;
PARAMETER(PAR.Company_Number);
PARAMETER(PAR.AR_Customer_Number);
PARAMETER(PAR.YEAR_1_USR);
PARAMETER(PAR.Month_1_USR);
PARAMETER(PAR.Days_to_Pay_1_USR);
{
 //?USER: Initialize routine

 // WRK.Count USR = CON.*ZERO
 WRK.Count_USR = 0;

 // WRK.Days to Pay = CON.*ZERO
 WRK.Days_to_Pay = *ZERO;

 //?***** Change Log and Comments
 //?** 9/12/2006 LJB L60906C
 //?** Calculations are different if company number is zeros.
 //?** Accumulate "Days to Pay" and Bal Invoices Paid
 //?** then divide the accum'd days to pay by the accum'd invoices
 //?** paid in the exit processing user point.
 //?*****
 //?USER: Processing if Data record not found

 // PAR.Bal Avg Days to Pay = CON.*ZERO
 PAR.Bal_Avg_Days_to_Pay = *ZERO;

 //?USER: Process Data record

 CASE;

 // IF PAR.Company Number is not equal to zero
 IF PAR.Company_Number <> *ZERO;

 CASE;

 // IF DB1.Company Number EQ PAR.Company Number
 IF DB1.Company_Number = PAR.Company_Number;

 // PAR.Bal Avg Days to Pay = DB1.Bal Avg Days to Pay
 PAR.Bal_Avg_Days_to_Pay = DB1.Bal_Avg_Days_to_Pay;

 // WRK.Count USR = CON.1
 WRK.Count_USR = 1;

 ENDIF;

 // IF *OTHERWISE
 IF *OTHERWISE;

 //?** Accum Days to Pay and Bal Invoices Paid  9/12/2006 LJB L60906C
 // WRK.Days to Pay = WRK.Days to Pay + DB1.Days to Pay
 WRK.Days_to_Pay = WRK.Days_to_Pay + DB1.Days_to_Pay;

 // WRK.Count USR = WRK.Count USR + DB1.Bal Nbr of Invoices Paid
 WRK.Count_USR = WRK.Count_USR + DB1.Bal_Nbr_of_Invoices_Paid;

 ENDIF;

 //?USER: Exit processing

 //?** Calculations are different if company number is zeros.
 //?** 9/12/2006 LJB
 //?** Calcs are different if Company is zeros or passed in
 CASE;

 // IF PAR.Company Number is not equal to zero
 IF PAR.Company_Number <> *ZERO;

 //?** Average days to pay if company is passed in
 CASE;

 // IF WRK.Count USR is GT 0
 IF WRK.Count_USR > *ZERO;

 // PAR.Bal Avg Days to Pay = PAR.Bal Avg Days to Pay / WRK.Count USR *Rounded
 PAR.Bal_Avg_Days_to_Pay = PAR.Bal_Avg_Days_to_Pay / WRK.Count_USR 'H';

 ENDIF;

 //?** Calcs are different if Company is zeros or passed in
 // IF *OTHERWISE
 IF *OTHERWISE;

 //?** Average days to pay if calcs are for all companies
 CASE;

 // IF WRK.Count USR is GT 0
 IF WRK.Count_USR > *ZERO;

 // PAR.Bal Avg Days to Pay = WRK.Days to Pay / WRK.Count USR *Rounded
 PAR.Bal_Avg_Days_to_Pay = WRK.Days_to_Pay / WRK.Count_USR 'H';

 //?** Average days to pay if calcs are for all companies
 // IF *OTHERWISE
 IF *OTHERWISE;

 ENDIF;

 ENDIF;

}


EXECUTE FUNCTION(Rtv Past AvgDys Pay   RT) TYPE(RTVOBJ) FILE(PDLDCPP)           AC1658492;
PARAMETER(PAR.Company_Number);
PARAMETER(PAR.AR_Customer_Number);
PARAMETER(PAR.YEAR_2_USR);
PARAMETER(PAR.Month_2_USR);
PARAMETER(PAR.Days_to_Pay_2_USR);
{
 //?USER: Initialize routine

 // WRK.Count USR = CON.*ZERO
 WRK.Count_USR = 0;

 // WRK.Days to Pay = CON.*ZERO
 WRK.Days_to_Pay = *ZERO;

 //?***** Change Log and Comments
 //?** 9/12/2006 LJB L60906C
 //?** Calculations are different if company number is zeros.
 //?** Accumulate "Days to Pay" and Bal Invoices Paid
 //?** then divide the accum'd days to pay by the accum'd invoices
 //?** paid in the exit processing user point.
 //?*****
 //?USER: Processing if Data record not found

 // PAR.Bal Avg Days to Pay = CON.*ZERO
 PAR.Bal_Avg_Days_to_Pay = *ZERO;

 //?USER: Process Data record

 CASE;

 // IF PAR.Company Number is not equal to zero
 IF PAR.Company_Number <> *ZERO;

 CASE;

 // IF DB1.Company Number EQ PAR.Company Number
 IF DB1.Company_Number = PAR.Company_Number;

 // PAR.Bal Avg Days to Pay = DB1.Bal Avg Days to Pay
 PAR.Bal_Avg_Days_to_Pay = DB1.Bal_Avg_Days_to_Pay;

 // WRK.Count USR = CON.1
 WRK.Count_USR = 1;

 ENDIF;

 // IF *OTHERWISE
 IF *OTHERWISE;

 //?** Accum Days to Pay and Bal Invoices Paid  9/12/2006 LJB L60906C
 // WRK.Days to Pay = WRK.Days to Pay + DB1.Days to Pay
 WRK.Days_to_Pay = WRK.Days_to_Pay + DB1.Days_to_Pay;

 // WRK.Count USR = WRK.Count USR + DB1.Bal Nbr of Invoices Paid
 WRK.Count_USR = WRK.Count_USR + DB1.Bal_Nbr_of_Invoices_Paid;

 ENDIF;

 //?USER: Exit processing

 //?** Calculations are different if company number is zeros.
 //?** 9/12/2006 LJB
 //?** Calcs are different if Company is zeros or passed in
 CASE;

 // IF PAR.Company Number is not equal to zero
 IF PAR.Company_Number <> *ZERO;

 //?** Average days to pay if company is passed in
 CASE;

 // IF WRK.Count USR is GT 0
 IF WRK.Count_USR > *ZERO;

 // PAR.Bal Avg Days to Pay = PAR.Bal Avg Days to Pay / WRK.Count USR *Rounded
 PAR.Bal_Avg_Days_to_Pay = PAR.Bal_Avg_Days_to_Pay / WRK.Count_USR 'H';

 ENDIF;

 //?** Calcs are different if Company is zeros or passed in
 // IF *OTHERWISE
 IF *OTHERWISE;

 //?** Average days to pay if calcs are for all companies
 CASE;

 // IF WRK.Count USR is GT 0
 IF WRK.Count_USR > *ZERO;

 // PAR.Bal Avg Days to Pay = WRK.Days to Pay / WRK.Count USR *Rounded
 PAR.Bal_Avg_Days_to_Pay = WRK.Days_to_Pay / WRK.Count_USR 'H';

 //?** Average days to pay if calcs are for all companies
 // IF *OTHERWISE
 IF *OTHERWISE;

 ENDIF;

 ENDIF;

}


EXECUTE FUNCTION(Rtv Past AvgDys Pay   RT) TYPE(RTVOBJ) FILE(PDLDCPP)           AC1658492;
PARAMETER(PAR.Company_Number);
PARAMETER(PAR.AR_Customer_Number);
PARAMETER(PAR.YEAR_3_USR);
PARAMETER(PAR.Month_3_USR);
PARAMETER(PAR.Days_to_Pay_3_USR);
{
 //?USER: Initialize routine

 // WRK.Count USR = CON.*ZERO
 WRK.Count_USR = 0;

 // WRK.Days to Pay = CON.*ZERO
 WRK.Days_to_Pay = *ZERO;

 //?***** Change Log and Comments
 //?** 9/12/2006 LJB L60906C
 //?** Calculations are different if company number is zeros.
 //?** Accumulate "Days to Pay" and Bal Invoices Paid
 //?** then divide the accum'd days to pay by the accum'd invoices
 //?** paid in the exit processing user point.
 //?*****
 //?USER: Processing if Data record not found

 // PAR.Bal Avg Days to Pay = CON.*ZERO
 PAR.Bal_Avg_Days_to_Pay = *ZERO;

 //?USER: Process Data record

 CASE;

 // IF PAR.Company Number is not equal to zero
 IF PAR.Company_Number <> *ZERO;

 CASE;

 // IF DB1.Company Number EQ PAR.Company Number
 IF DB1.Company_Number = PAR.Company_Number;

 // PAR.Bal Avg Days to Pay = DB1.Bal Avg Days to Pay
 PAR.Bal_Avg_Days_to_Pay = DB1.Bal_Avg_Days_to_Pay;

 // WRK.Count USR = CON.1
 WRK.Count_USR = 1;

 ENDIF;

 // IF *OTHERWISE
 IF *OTHERWISE;

 //?** Accum Days to Pay and Bal Invoices Paid  9/12/2006 LJB L60906C
 // WRK.Days to Pay = WRK.Days to Pay + DB1.Days to Pay
 WRK.Days_to_Pay = WRK.Days_to_Pay + DB1.Days_to_Pay;

 // WRK.Count USR = WRK.Count USR + DB1.Bal Nbr of Invoices Paid
 WRK.Count_USR = WRK.Count_USR + DB1.Bal_Nbr_of_Invoices_Paid;

 ENDIF;

 //?USER: Exit processing

 //?** Calculations are different if company number is zeros.
 //?** 9/12/2006 LJB
 //?** Calcs are different if Company is zeros or passed in
 CASE;

 // IF PAR.Company Number is not equal to zero
 IF PAR.Company_Number <> *ZERO;

 //?** Average days to pay if company is passed in
 CASE;

 // IF WRK.Count USR is GT 0
 IF WRK.Count_USR > *ZERO;

 // PAR.Bal Avg Days to Pay = PAR.Bal Avg Days to Pay / WRK.Count USR *Rounded
 PAR.Bal_Avg_Days_to_Pay = PAR.Bal_Avg_Days_to_Pay / WRK.Count_USR 'H';

 ENDIF;

 //?** Calcs are different if Company is zeros or passed in
 // IF *OTHERWISE
 IF *OTHERWISE;

 //?** Average days to pay if calcs are for all companies
 CASE;

 // IF WRK.Count USR is GT 0
 IF WRK.Count_USR > *ZERO;

 // PAR.Bal Avg Days to Pay = WRK.Days to Pay / WRK.Count USR *Rounded
 PAR.Bal_Avg_Days_to_Pay = WRK.Days_to_Pay / WRK.Count_USR 'H';

 //?** Average days to pay if calcs are for all companies
 // IF *OTHERWISE
 IF *OTHERWISE;

 ENDIF;

 ENDIF;

}


EXECUTE FUNCTION(Rtv Past AvgDys Pay   RT) TYPE(RTVOBJ) FILE(PDLDCPP)           AC1658492;
PARAMETER(PAR.Company_Number);
PARAMETER(PAR.AR_Customer_Number);
PARAMETER(PAR.YEAR_4_USR);
PARAMETER(PAR.Month_4_USR);
PARAMETER(PAR.Days_to_Pay_4_USR);
{
 //?USER: Initialize routine

 // WRK.Count USR = CON.*ZERO
 WRK.Count_USR = 0;

 // WRK.Days to Pay = CON.*ZERO
 WRK.Days_to_Pay = *ZERO;

 //?***** Change Log and Comments
 //?** 9/12/2006 LJB L60906C
 //?** Calculations are different if company number is zeros.
 //?** Accumulate "Days to Pay" and Bal Invoices Paid
 //?** then divide the accum'd days to pay by the accum'd invoices
 //?** paid in the exit processing user point.
 //?*****
 //?USER: Processing if Data record not found

 // PAR.Bal Avg Days to Pay = CON.*ZERO
 PAR.Bal_Avg_Days_to_Pay = *ZERO;

 //?USER: Process Data record

 CASE;

 // IF PAR.Company Number is not equal to zero
 IF PAR.Company_Number <> *ZERO;

 CASE;

 // IF DB1.Company Number EQ PAR.Company Number
 IF DB1.Company_Number = PAR.Company_Number;

 // PAR.Bal Avg Days to Pay = DB1.Bal Avg Days to Pay
 PAR.Bal_Avg_Days_to_Pay = DB1.Bal_Avg_Days_to_Pay;

 // WRK.Count USR = CON.1
 WRK.Count_USR = 1;

 ENDIF;

 // IF *OTHERWISE
 IF *OTHERWISE;

 //?** Accum Days to Pay and Bal Invoices Paid  9/12/2006 LJB L60906C
 // WRK.Days to Pay = WRK.Days to Pay + DB1.Days to Pay
 WRK.Days_to_Pay = WRK.Days_to_Pay + DB1.Days_to_Pay;

 // WRK.Count USR = WRK.Count USR + DB1.Bal Nbr of Invoices Paid
 WRK.Count_USR = WRK.Count_USR + DB1.Bal_Nbr_of_Invoices_Paid;

 ENDIF;

 //?USER: Exit processing

 //?** Calculations are different if company number is zeros.
 //?** 9/12/2006 LJB
 //?** Calcs are different if Company is zeros or passed in
 CASE;

 // IF PAR.Company Number is not equal to zero
 IF PAR.Company_Number <> *ZERO;

 //?** Average days to pay if company is passed in
 CASE;

 // IF WRK.Count USR is GT 0
 IF WRK.Count_USR > *ZERO;

 // PAR.Bal Avg Days to Pay = PAR.Bal Avg Days to Pay / WRK.Count USR *Rounded
 PAR.Bal_Avg_Days_to_Pay = PAR.Bal_Avg_Days_to_Pay / WRK.Count_USR 'H';

 ENDIF;

 //?** Calcs are different if Company is zeros or passed in
 // IF *OTHERWISE
 IF *OTHERWISE;

 //?** Average days to pay if calcs are for all companies
 CASE;

 // IF WRK.Count USR is GT 0
 IF WRK.Count_USR > *ZERO;

 // PAR.Bal Avg Days to Pay = WRK.Days to Pay / WRK.Count USR *Rounded
 PAR.Bal_Avg_Days_to_Pay = WRK.Days_to_Pay / WRK.Count_USR 'H';

 //?** Average days to pay if calcs are for all companies
 // IF *OTHERWISE
 IF *OTHERWISE;

 ENDIF;

 ENDIF;

}


EXECUTE FUNCTION(Rtv Past AvgDys Pay   RT) TYPE(RTVOBJ) FILE(PDLDCPP)           AC1658492;
PARAMETER(PAR.Company_Number);
PARAMETER(PAR.AR_Customer_Number);
PARAMETER(PAR.YEAR_5_USR);
PARAMETER(PAR.Month_5_USR);
PARAMETER(PAR.Days_to_Pay_5_USR);
{
 //?USER: Initialize routine

 // WRK.Count USR = CON.*ZERO
 WRK.Count_USR = 0;

 // WRK.Days to Pay = CON.*ZERO
 WRK.Days_to_Pay = *ZERO;

 //?***** Change Log and Comments
 //?** 9/12/2006 LJB L60906C
 //?** Calculations are different if company number is zeros.
 //?** Accumulate "Days to Pay" and Bal Invoices Paid
 //?** then divide the accum'd days to pay by the accum'd invoices
 //?** paid in the exit processing user point.
 //?*****
 //?USER: Processing if Data record not found

 // PAR.Bal Avg Days to Pay = CON.*ZERO
 PAR.Bal_Avg_Days_to_Pay = *ZERO;

 //?USER: Process Data record

 CASE;

 // IF PAR.Company Number is not equal to zero
 IF PAR.Company_Number <> *ZERO;

 CASE;

 // IF DB1.Company Number EQ PAR.Company Number
 IF DB1.Company_Number = PAR.Company_Number;

 // PAR.Bal Avg Days to Pay = DB1.Bal Avg Days to Pay
 PAR.Bal_Avg_Days_to_Pay = DB1.Bal_Avg_Days_to_Pay;

 // WRK.Count USR = CON.1
 WRK.Count_USR = 1;

 ENDIF;

 // IF *OTHERWISE
 IF *OTHERWISE;

 //?** Accum Days to Pay and Bal Invoices Paid  9/12/2006 LJB L60906C
 // WRK.Days to Pay = WRK.Days to Pay + DB1.Days to Pay
 WRK.Days_to_Pay = WRK.Days_to_Pay + DB1.Days_to_Pay;

 // WRK.Count USR = WRK.Count USR + DB1.Bal Nbr of Invoices Paid
 WRK.Count_USR = WRK.Count_USR + DB1.Bal_Nbr_of_Invoices_Paid;

 ENDIF;

 //?USER: Exit processing

 //?** Calculations are different if company number is zeros.
 //?** 9/12/2006 LJB
 //?** Calcs are different if Company is zeros or passed in
 CASE;

 // IF PAR.Company Number is not equal to zero
 IF PAR.Company_Number <> *ZERO;

 //?** Average days to pay if company is passed in
 CASE;

 // IF WRK.Count USR is GT 0
 IF WRK.Count_USR > *ZERO;

 // PAR.Bal Avg Days to Pay = PAR.Bal Avg Days to Pay / WRK.Count USR *Rounded
 PAR.Bal_Avg_Days_to_Pay = PAR.Bal_Avg_Days_to_Pay / WRK.Count_USR 'H';

 ENDIF;

 //?** Calcs are different if Company is zeros or passed in
 // IF *OTHERWISE
 IF *OTHERWISE;

 //?** Average days to pay if calcs are for all companies
 CASE;

 // IF WRK.Count USR is GT 0
 IF WRK.Count_USR > *ZERO;

 // PAR.Bal Avg Days to Pay = WRK.Days to Pay / WRK.Count USR *Rounded
 PAR.Bal_Avg_Days_to_Pay = WRK.Days_to_Pay / WRK.Count_USR 'H';

 //?** Average days to pay if calcs are for all companies
 // IF *OTHERWISE
 IF *OTHERWISE;

 ENDIF;

 ENDIF;

}


EXECUTE FUNCTION(Rtv Past AvgDys Pay   RT) TYPE(RTVOBJ) FILE(PDLDCPP)           AC1658492;
PARAMETER(PAR.Company_Number);
PARAMETER(PAR.AR_Customer_Number);
PARAMETER(PAR.YEAR_6_USR);
PARAMETER(PAR.Month_6_USR);
PARAMETER(PAR.Days_to_Pay_6_USR);
{
 //?USER: Initialize routine

 // WRK.Count USR = CON.*ZERO
 WRK.Count_USR = 0;

 // WRK.Days to Pay = CON.*ZERO
 WRK.Days_to_Pay = *ZERO;

 //?***** Change Log and Comments
 //?** 9/12/2006 LJB L60906C
 //?** Calculations are different if company number is zeros.
 //?** Accumulate "Days to Pay" and Bal Invoices Paid
 //?** then divide the accum'd days to pay by the accum'd invoices
 //?** paid in the exit processing user point.
 //?*****
 //?USER: Processing if Data record not found

 // PAR.Bal Avg Days to Pay = CON.*ZERO
 PAR.Bal_Avg_Days_to_Pay = *ZERO;

 //?USER: Process Data record

 CASE;

 // IF PAR.Company Number is not equal to zero
 IF PAR.Company_Number <> *ZERO;

 CASE;

 // IF DB1.Company Number EQ PAR.Company Number
 IF DB1.Company_Number = PAR.Company_Number;

 // PAR.Bal Avg Days to Pay = DB1.Bal Avg Days to Pay
 PAR.Bal_Avg_Days_to_Pay = DB1.Bal_Avg_Days_to_Pay;

 // WRK.Count USR = CON.1
 WRK.Count_USR = 1;

 ENDIF;

 // IF *OTHERWISE
 IF *OTHERWISE;

 //?** Accum Days to Pay and Bal Invoices Paid  9/12/2006 LJB L60906C
 // WRK.Days to Pay = WRK.Days to Pay + DB1.Days to Pay
 WRK.Days_to_Pay = WRK.Days_to_Pay + DB1.Days_to_Pay;

 // WRK.Count USR = WRK.Count USR + DB1.Bal Nbr of Invoices Paid
 WRK.Count_USR = WRK.Count_USR + DB1.Bal_Nbr_of_Invoices_Paid;

 ENDIF;

 //?USER: Exit processing

 //?** Calculations are different if company number is zeros.
 //?** 9/12/2006 LJB
 //?** Calcs are different if Company is zeros or passed in
 CASE;

 // IF PAR.Company Number is not equal to zero
 IF PAR.Company_Number <> *ZERO;

 //?** Average days to pay if company is passed in
 CASE;

 // IF WRK.Count USR is GT 0
 IF WRK.Count_USR > *ZERO;

 // PAR.Bal Avg Days to Pay = PAR.Bal Avg Days to Pay / WRK.Count USR *Rounded
 PAR.Bal_Avg_Days_to_Pay = PAR.Bal_Avg_Days_to_Pay / WRK.Count_USR 'H';

 ENDIF;

 //?** Calcs are different if Company is zeros or passed in
 // IF *OTHERWISE
 IF *OTHERWISE;

 //?** Average days to pay if calcs are for all companies
 CASE;

 // IF WRK.Count USR is GT 0
 IF WRK.Count_USR > *ZERO;

 // PAR.Bal Avg Days to Pay = WRK.Days to Pay / WRK.Count USR *Rounded
 PAR.Bal_Avg_Days_to_Pay = WRK.Days_to_Pay / WRK.Count_USR 'H';

 //?** Average days to pay if calcs are for all companies
 // IF *OTHERWISE
 IF *OTHERWISE;

 ENDIF;

 ENDIF;

}


EXECUTE FUNCTION(Rtv Past AvgDys Pay   RT) TYPE(RTVOBJ) FILE(PDLDCPP)           AC1658492;
PARAMETER(PAR.Company_Number);
PARAMETER(PAR.AR_Customer_Number);
PARAMETER(PAR.YEAR_7_USR);
PARAMETER(PAR.Month_7_USR);
PARAMETER(PAR.Days_to_Pay_7_USR);
{
 //?USER: Initialize routine

 // WRK.Count USR = CON.*ZERO
 WRK.Count_USR = 0;

 // WRK.Days to Pay = CON.*ZERO
 WRK.Days_to_Pay = *ZERO;

 //?***** Change Log and Comments
 //?** 9/12/2006 LJB L60906C
 //?** Calculations are different if company number is zeros.
 //?** Accumulate "Days to Pay" and Bal Invoices Paid
 //?** then divide the accum'd days to pay by the accum'd invoices
 //?** paid in the exit processing user point.
 //?*****
 //?USER: Processing if Data record not found

 // PAR.Bal Avg Days to Pay = CON.*ZERO
 PAR.Bal_Avg_Days_to_Pay = *ZERO;

 //?USER: Process Data record

 CASE;

 // IF PAR.Company Number is not equal to zero
 IF PAR.Company_Number <> *ZERO;

 CASE;

 // IF DB1.Company Number EQ PAR.Company Number
 IF DB1.Company_Number = PAR.Company_Number;

 // PAR.Bal Avg Days to Pay = DB1.Bal Avg Days to Pay
 PAR.Bal_Avg_Days_to_Pay = DB1.Bal_Avg_Days_to_Pay;

 // WRK.Count USR = CON.1
 WRK.Count_USR = 1;

 ENDIF;

 // IF *OTHERWISE
 IF *OTHERWISE;

 //?** Accum Days to Pay and Bal Invoices Paid  9/12/2006 LJB L60906C
 // WRK.Days to Pay = WRK.Days to Pay + DB1.Days to Pay
 WRK.Days_to_Pay = WRK.Days_to_Pay + DB1.Days_to_Pay;

 // WRK.Count USR = WRK.Count USR + DB1.Bal Nbr of Invoices Paid
 WRK.Count_USR = WRK.Count_USR + DB1.Bal_Nbr_of_Invoices_Paid;

 ENDIF;

 //?USER: Exit processing

 //?** Calculations are different if company number is zeros.
 //?** 9/12/2006 LJB
 //?** Calcs are different if Company is zeros or passed in
 CASE;

 // IF PAR.Company Number is not equal to zero
 IF PAR.Company_Number <> *ZERO;

 //?** Average days to pay if company is passed in
 CASE;

 // IF WRK.Count USR is GT 0
 IF WRK.Count_USR > *ZERO;

 // PAR.Bal Avg Days to Pay = PAR.Bal Avg Days to Pay / WRK.Count USR *Rounded
 PAR.Bal_Avg_Days_to_Pay = PAR.Bal_Avg_Days_to_Pay / WRK.Count_USR 'H';

 ENDIF;

 //?** Calcs are different if Company is zeros or passed in
 // IF *OTHERWISE
 IF *OTHERWISE;

 //?** Average days to pay if calcs are for all companies
 CASE;

 // IF WRK.Count USR is GT 0
 IF WRK.Count_USR > *ZERO;

 // PAR.Bal Avg Days to Pay = WRK.Days to Pay / WRK.Count USR *Rounded
 PAR.Bal_Avg_Days_to_Pay = WRK.Days_to_Pay / WRK.Count_USR 'H';

 //?** Average days to pay if calcs are for all companies
 // IF *OTHERWISE
 IF *OTHERWISE;

 ENDIF;

 ENDIF;

}


EXECUTE FUNCTION(Rtv Past AvgDys Pay   RT) TYPE(RTVOBJ) FILE(PDLDCPP)           AC1658492;
PARAMETER(PAR.Company_Number);
PARAMETER(PAR.AR_Customer_Number);
PARAMETER(PAR.YEAR_8_USR);
PARAMETER(PAR.Month_8_USR);
PARAMETER(PAR.Days_to_Pay_8_USR);
{
 //?USER: Initialize routine

 // WRK.Count USR = CON.*ZERO
 WRK.Count_USR = 0;

 // WRK.Days to Pay = CON.*ZERO
 WRK.Days_to_Pay = *ZERO;

 //?***** Change Log and Comments
 //?** 9/12/2006 LJB L60906C
 //?** Calculations are different if company number is zeros.
 //?** Accumulate "Days to Pay" and Bal Invoices Paid
 //?** then divide the accum'd days to pay by the accum'd invoices
 //?** paid in the exit processing user point.
 //?*****
 //?USER: Processing if Data record not found

 // PAR.Bal Avg Days to Pay = CON.*ZERO
 PAR.Bal_Avg_Days_to_Pay = *ZERO;

 //?USER: Process Data record

 CASE;

 // IF PAR.Company Number is not equal to zero
 IF PAR.Company_Number <> *ZERO;

 CASE;

 // IF DB1.Company Number EQ PAR.Company Number
 IF DB1.Company_Number = PAR.Company_Number;

 // PAR.Bal Avg Days to Pay = DB1.Bal Avg Days to Pay
 PAR.Bal_Avg_Days_to_Pay = DB1.Bal_Avg_Days_to_Pay;

 // WRK.Count USR = CON.1
 WRK.Count_USR = 1;

 ENDIF;

 // IF *OTHERWISE
 IF *OTHERWISE;

 //?** Accum Days to Pay and Bal Invoices Paid  9/12/2006 LJB L60906C
 // WRK.Days to Pay = WRK.Days to Pay + DB1.Days to Pay
 WRK.Days_to_Pay = WRK.Days_to_Pay + DB1.Days_to_Pay;

 // WRK.Count USR = WRK.Count USR + DB1.Bal Nbr of Invoices Paid
 WRK.Count_USR = WRK.Count_USR + DB1.Bal_Nbr_of_Invoices_Paid;

 ENDIF;

 //?USER: Exit processing

 //?** Calculations are different if company number is zeros.
 //?** 9/12/2006 LJB
 //?** Calcs are different if Company is zeros or passed in
 CASE;

 // IF PAR.Company Number is not equal to zero
 IF PAR.Company_Number <> *ZERO;

 //?** Average days to pay if company is passed in
 CASE;

 // IF WRK.Count USR is GT 0
 IF WRK.Count_USR > *ZERO;

 // PAR.Bal Avg Days to Pay = PAR.Bal Avg Days to Pay / WRK.Count USR *Rounded
 PAR.Bal_Avg_Days_to_Pay = PAR.Bal_Avg_Days_to_Pay / WRK.Count_USR 'H';

 ENDIF;

 //?** Calcs are different if Company is zeros or passed in
 // IF *OTHERWISE
 IF *OTHERWISE;

 //?** Average days to pay if calcs are for all companies
 CASE;

 // IF WRK.Count USR is GT 0
 IF WRK.Count_USR > *ZERO;

 // PAR.Bal Avg Days to Pay = WRK.Days to Pay / WRK.Count USR *Rounded
 PAR.Bal_Avg_Days_to_Pay = WRK.Days_to_Pay / WRK.Count_USR 'H';

 //?** Average days to pay if calcs are for all companies
 // IF *OTHERWISE
 IF *OTHERWISE;

 ENDIF;

 ENDIF;

}


EXECUTE FUNCTION(Rtv Past AvgDys Pay   RT) TYPE(RTVOBJ) FILE(PDLDCPP)           AC1658492;
PARAMETER(PAR.Company_Number);
PARAMETER(PAR.AR_Customer_Number);
PARAMETER(PAR.YEAR_9_USR);
PARAMETER(PAR.Month_9_USR);
PARAMETER(PAR.Days_to_Pay_9_USR);
{
 //?USER: Initialize routine

 // WRK.Count USR = CON.*ZERO
 WRK.Count_USR = 0;

 // WRK.Days to Pay = CON.*ZERO
 WRK.Days_to_Pay = *ZERO;

 //?***** Change Log and Comments
 //?** 9/12/2006 LJB L60906C
 //?** Calculations are different if company number is zeros.
 //?** Accumulate "Days to Pay" and Bal Invoices Paid
 //?** then divide the accum'd days to pay by the accum'd invoices
 //?** paid in the exit processing user point.
 //?*****
 //?USER: Processing if Data record not found

 // PAR.Bal Avg Days to Pay = CON.*ZERO
 PAR.Bal_Avg_Days_to_Pay = *ZERO;

 //?USER: Process Data record

 CASE;

 // IF PAR.Company Number is not equal to zero
 IF PAR.Company_Number <> *ZERO;

 CASE;

 // IF DB1.Company Number EQ PAR.Company Number
 IF DB1.Company_Number = PAR.Company_Number;

 // PAR.Bal Avg Days to Pay = DB1.Bal Avg Days to Pay
 PAR.Bal_Avg_Days_to_Pay = DB1.Bal_Avg_Days_to_Pay;

 // WRK.Count USR = CON.1
 WRK.Count_USR = 1;

 ENDIF;

 // IF *OTHERWISE
 IF *OTHERWISE;

 //?** Accum Days to Pay and Bal Invoices Paid  9/12/2006 LJB L60906C
 // WRK.Days to Pay = WRK.Days to Pay + DB1.Days to Pay
 WRK.Days_to_Pay = WRK.Days_to_Pay + DB1.Days_to_Pay;

 // WRK.Count USR = WRK.Count USR + DB1.Bal Nbr of Invoices Paid
 WRK.Count_USR = WRK.Count_USR + DB1.Bal_Nbr_of_Invoices_Paid;

 ENDIF;

 //?USER: Exit processing

 //?** Calculations are different if company number is zeros.
 //?** 9/12/2006 LJB
 //?** Calcs are different if Company is zeros or passed in
 CASE;

 // IF PAR.Company Number is not equal to zero
 IF PAR.Company_Number <> *ZERO;

 //?** Average days to pay if company is passed in
 CASE;

 // IF WRK.Count USR is GT 0
 IF WRK.Count_USR > *ZERO;

 // PAR.Bal Avg Days to Pay = PAR.Bal Avg Days to Pay / WRK.Count USR *Rounded
 PAR.Bal_Avg_Days_to_Pay = PAR.Bal_Avg_Days_to_Pay / WRK.Count_USR 'H';

 ENDIF;

 //?** Calcs are different if Company is zeros or passed in
 // IF *OTHERWISE
 IF *OTHERWISE;

 //?** Average days to pay if calcs are for all companies
 CASE;

 // IF WRK.Count USR is GT 0
 IF WRK.Count_USR > *ZERO;

 // PAR.Bal Avg Days to Pay = WRK.Days to Pay / WRK.Count USR *Rounded
 PAR.Bal_Avg_Days_to_Pay = WRK.Days_to_Pay / WRK.Count_USR 'H';

 //?** Average days to pay if calcs are for all companies
 // IF *OTHERWISE
 IF *OTHERWISE;

 ENDIF;

 ENDIF;

}


EXECUTE FUNCTION(Rtv Past AvgDys Pay   RT) TYPE(RTVOBJ) FILE(PDLDCPP)           AC1658492;
PARAMETER(PAR.Company_Number);
PARAMETER(PAR.AR_Customer_Number);
PARAMETER(PAR.YEAR_10_USR);
PARAMETER(PAR.Month_10_USR);
PARAMETER(PAR.Days_to_Pay_10_USR);
{
 //?USER: Initialize routine

 // WRK.Count USR = CON.*ZERO
 WRK.Count_USR = 0;

 // WRK.Days to Pay = CON.*ZERO
 WRK.Days_to_Pay = *ZERO;

 //?***** Change Log and Comments
 //?** 9/12/2006 LJB L60906C
 //?** Calculations are different if company number is zeros.
 //?** Accumulate "Days to Pay" and Bal Invoices Paid
 //?** then divide the accum'd days to pay by the accum'd invoices
 //?** paid in the exit processing user point.
 //?*****
 //?USER: Processing if Data record not found

 // PAR.Bal Avg Days to Pay = CON.*ZERO
 PAR.Bal_Avg_Days_to_Pay = *ZERO;

 //?USER: Process Data record

 CASE;

 // IF PAR.Company Number is not equal to zero
 IF PAR.Company_Number <> *ZERO;

 CASE;

 // IF DB1.Company Number EQ PAR.Company Number
 IF DB1.Company_Number = PAR.Company_Number;

 // PAR.Bal Avg Days to Pay = DB1.Bal Avg Days to Pay
 PAR.Bal_Avg_Days_to_Pay = DB1.Bal_Avg_Days_to_Pay;

 // WRK.Count USR = CON.1
 WRK.Count_USR = 1;

 ENDIF;

 // IF *OTHERWISE
 IF *OTHERWISE;

 //?** Accum Days to Pay and Bal Invoices Paid  9/12/2006 LJB L60906C
 // WRK.Days to Pay = WRK.Days to Pay + DB1.Days to Pay
 WRK.Days_to_Pay = WRK.Days_to_Pay + DB1.Days_to_Pay;

 // WRK.Count USR = WRK.Count USR + DB1.Bal Nbr of Invoices Paid
 WRK.Count_USR = WRK.Count_USR + DB1.Bal_Nbr_of_Invoices_Paid;

 ENDIF;

 //?USER: Exit processing

 //?** Calculations are different if company number is zeros.
 //?** 9/12/2006 LJB
 //?** Calcs are different if Company is zeros or passed in
 CASE;

 // IF PAR.Company Number is not equal to zero
 IF PAR.Company_Number <> *ZERO;

 //?** Average days to pay if company is passed in
 CASE;

 // IF WRK.Count USR is GT 0
 IF WRK.Count_USR > *ZERO;

 // PAR.Bal Avg Days to Pay = PAR.Bal Avg Days to Pay / WRK.Count USR *Rounded
 PAR.Bal_Avg_Days_to_Pay = PAR.Bal_Avg_Days_to_Pay / WRK.Count_USR 'H';

 ENDIF;

 //?** Calcs are different if Company is zeros or passed in
 // IF *OTHERWISE
 IF *OTHERWISE;

 //?** Average days to pay if calcs are for all companies
 CASE;

 // IF WRK.Count USR is GT 0
 IF WRK.Count_USR > *ZERO;

 // PAR.Bal Avg Days to Pay = WRK.Days to Pay / WRK.Count USR *Rounded
 PAR.Bal_Avg_Days_to_Pay = WRK.Days_to_Pay / WRK.Count_USR 'H';

 //?** Average days to pay if calcs are for all companies
 // IF *OTHERWISE
 IF *OTHERWISE;

 ENDIF;

 ENDIF;

}


EXECUTE FUNCTION(Rtv Past AvgDys Pay   RT) TYPE(RTVOBJ) FILE(PDLDCPP)           AC1658492;
PARAMETER(PAR.Company_Number);
PARAMETER(PAR.AR_Customer_Number);
PARAMETER(PAR.YEAR_11_USR);
PARAMETER(PAR.Month_11_USR);
PARAMETER(PAR.Days_to_Pay_11_USR);
{
 //?USER: Initialize routine

 // WRK.Count USR = CON.*ZERO
 WRK.Count_USR = 0;

 // WRK.Days to Pay = CON.*ZERO
 WRK.Days_to_Pay = *ZERO;

 //?***** Change Log and Comments
 //?** 9/12/2006 LJB L60906C
 //?** Calculations are different if company number is zeros.
 //?** Accumulate "Days to Pay" and Bal Invoices Paid
 //?** then divide the accum'd days to pay by the accum'd invoices
 //?** paid in the exit processing user point.
 //?*****
 //?USER: Processing if Data record not found

 // PAR.Bal Avg Days to Pay = CON.*ZERO
 PAR.Bal_Avg_Days_to_Pay = *ZERO;

 //?USER: Process Data record

 CASE;

 // IF PAR.Company Number is not equal to zero
 IF PAR.Company_Number <> *ZERO;

 CASE;

 // IF DB1.Company Number EQ PAR.Company Number
 IF DB1.Company_Number = PAR.Company_Number;

 // PAR.Bal Avg Days to Pay = DB1.Bal Avg Days to Pay
 PAR.Bal_Avg_Days_to_Pay = DB1.Bal_Avg_Days_to_Pay;

 // WRK.Count USR = CON.1
 WRK.Count_USR = 1;

 ENDIF;

 // IF *OTHERWISE
 IF *OTHERWISE;

 //?** Accum Days to Pay and Bal Invoices Paid  9/12/2006 LJB L60906C
 // WRK.Days to Pay = WRK.Days to Pay + DB1.Days to Pay
 WRK.Days_to_Pay = WRK.Days_to_Pay + DB1.Days_to_Pay;

 // WRK.Count USR = WRK.Count USR + DB1.Bal Nbr of Invoices Paid
 WRK.Count_USR = WRK.Count_USR + DB1.Bal_Nbr_of_Invoices_Paid;

 ENDIF;

 //?USER: Exit processing

 //?** Calculations are different if company number is zeros.
 //?** 9/12/2006 LJB
 //?** Calcs are different if Company is zeros or passed in
 CASE;

 // IF PAR.Company Number is not equal to zero
 IF PAR.Company_Number <> *ZERO;

 //?** Average days to pay if company is passed in
 CASE;

 // IF WRK.Count USR is GT 0
 IF WRK.Count_USR > *ZERO;

 // PAR.Bal Avg Days to Pay = PAR.Bal Avg Days to Pay / WRK.Count USR *Rounded
 PAR.Bal_Avg_Days_to_Pay = PAR.Bal_Avg_Days_to_Pay / WRK.Count_USR 'H';

 ENDIF;

 //?** Calcs are different if Company is zeros or passed in
 // IF *OTHERWISE
 IF *OTHERWISE;

 //?** Average days to pay if calcs are for all companies
 CASE;

 // IF WRK.Count USR is GT 0
 IF WRK.Count_USR > *ZERO;

 // PAR.Bal Avg Days to Pay = WRK.Days to Pay / WRK.Count USR *Rounded
 PAR.Bal_Avg_Days_to_Pay = WRK.Days_to_Pay / WRK.Count_USR 'H';

 //?** Average days to pay if calcs are for all companies
 // IF *OTHERWISE
 IF *OTHERWISE;

 ENDIF;

 ENDIF;

}


EXECUTE FUNCTION(Rtv Past AvgDys Pay   RT) TYPE(RTVOBJ) FILE(PDLDCPP)           AC1658492;
PARAMETER(PAR.Company_Number);
PARAMETER(PAR.AR_Customer_Number);
PARAMETER(PAR.YEAR_12_USR);
PARAMETER(PAR.Month_12_USR);
PARAMETER(PAR.Days_to_Pay_12_USR);
{
 //?USER: Initialize routine

 // WRK.Count USR = CON.*ZERO
 WRK.Count_USR = 0;

 // WRK.Days to Pay = CON.*ZERO
 WRK.Days_to_Pay = *ZERO;

 //?***** Change Log and Comments
 //?** 9/12/2006 LJB L60906C
 //?** Calculations are different if company number is zeros.
 //?** Accumulate "Days to Pay" and Bal Invoices Paid
 //?** then divide the accum'd days to pay by the accum'd invoices
 //?** paid in the exit processing user point.
 //?*****
 //?USER: Processing if Data record not found

 // PAR.Bal Avg Days to Pay = CON.*ZERO
 PAR.Bal_Avg_Days_to_Pay = *ZERO;

 //?USER: Process Data record

 CASE;

 // IF PAR.Company Number is not equal to zero
 IF PAR.Company_Number <> *ZERO;

 CASE;

 // IF DB1.Company Number EQ PAR.Company Number
 IF DB1.Company_Number = PAR.Company_Number;

 // PAR.Bal Avg Days to Pay = DB1.Bal Avg Days to Pay
 PAR.Bal_Avg_Days_to_Pay = DB1.Bal_Avg_Days_to_Pay;

 // WRK.Count USR = CON.1
 WRK.Count_USR = 1;

 ENDIF;

 // IF *OTHERWISE
 IF *OTHERWISE;

 //?** Accum Days to Pay and Bal Invoices Paid  9/12/2006 LJB L60906C
 // WRK.Days to Pay = WRK.Days to Pay + DB1.Days to Pay
 WRK.Days_to_Pay = WRK.Days_to_Pay + DB1.Days_to_Pay;

 // WRK.Count USR = WRK.Count USR + DB1.Bal Nbr of Invoices Paid
 WRK.Count_USR = WRK.Count_USR + DB1.Bal_Nbr_of_Invoices_Paid;

 ENDIF;

 //?USER: Exit processing

 //?** Calculations are different if company number is zeros.
 //?** 9/12/2006 LJB
 //?** Calcs are different if Company is zeros or passed in
 CASE;

 // IF PAR.Company Number is not equal to zero
 IF PAR.Company_Number <> *ZERO;

 //?** Average days to pay if company is passed in
 CASE;

 // IF WRK.Count USR is GT 0
 IF WRK.Count_USR > *ZERO;

 // PAR.Bal Avg Days to Pay = PAR.Bal Avg Days to Pay / WRK.Count USR *Rounded
 PAR.Bal_Avg_Days_to_Pay = PAR.Bal_Avg_Days_to_Pay / WRK.Count_USR 'H';

 ENDIF;

 //?** Calcs are different if Company is zeros or passed in
 // IF *OTHERWISE
 IF *OTHERWISE;

 //?** Average days to pay if calcs are for all companies
 CASE;

 // IF WRK.Count USR is GT 0
 IF WRK.Count_USR > *ZERO;

 // PAR.Bal Avg Days to Pay = WRK.Days to Pay / WRK.Count USR *Rounded
 PAR.Bal_Avg_Days_to_Pay = WRK.Days_to_Pay / WRK.Count_USR 'H';

 //?** Average days to pay if calcs are for all companies
 // IF *OTHERWISE
 IF *OTHERWISE;

 ENDIF;

 ENDIF;

}


//?***** Change Log and Comments
DO;

//?***** 05/19/2006 LJB T00009
//?** Size of the Days to Pay User fields was changed from
//?** 3.0 to 5.0 to prevent truncating when accumulating for
//?** large volume customers.
//?*****
ENDDO;

