// ?------------------------------------------------------------------------------------------------
// ?Synon action diagram for CAXTSRR
// ?Date: 14.08.2025 Time: 03:26:39
// ?------------------------------------------------------------------------------------------------

//?USER: Initialize program

// WRK.User Profile Name = JOB.*USER
WRK.User_Profile_Name = JOB.*USER;

EXECUTE FUNCTION(RTV Pgm Setup          RT) TYPE(RTVOBJ) FILE(CADRREP)          AC1144204;
PARAMETER(WRK.User_Profile_Name);
PARAMETER(WRK.System_Date_Format);
PARAMETER(WRK.Allow_Multi_Company);
{
 //?USER: Process Data record

 MOVE *ALL (To: PAR From: DB1);

}


//?USER: Load subfile record from DBF record

//?Bypass records if allow multi-company is no
CASE;

// IF WRK.Allow Multi-Company is no
IF WRK.Allow_Multi_Company = 'N';

CASE;

// IF RCD.Company Number - 3 NE CTL.Company Number - 3
IF RCD.Company_Number <> CTL.Company_Number;

// PGM.*Record selected = CND.*NO
PGM.*Record_selected = 'N';

ENDIF;

ENDIF;

