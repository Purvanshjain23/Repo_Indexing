// ?------------------------------------------------------------------------------------------------
// ?Synon action diagram for PNVIXFR
// ?Date: 14.08.2025 Time: 03:46:41
// ?------------------------------------------------------------------------------------------------

//?Execute user function

//?ED Consignor Name
CASE;

// IF PAR.ED Consignor Name PREV NE PAR.ED Consignor Name
IF PAR.ED_Consignor_Name_PREV <> PAR.ED_Consignor_Name;

EXECUTE FUNCTION(Rtv Next Line Number  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009181;
PARAMETER(PAR.Company_Number);
PARAMETER(PAR.Order_Number);
PARAMETER(PAR.ED_Export_Document_Type);
PARAMETER(PAR.Country_Code);
PARAMETER(LCL.EDO_Comment_Line_No);
{
 //?USER: Processing if Data record not found

 // PAR.EDO Comment Line No = PAR.EDO Comment Line No + CON.001
 PAR.EDO_Comment_Line_No = PAR.EDO_Comment_Line_No + 001;

 //?USER: Process Data record

 // PAR.EDO Comment Line No = DB1.EDO Comment Line No + CON.001
 PAR.EDO_Comment_Line_No = DB1.EDO_Comment_Line_No + 001;

 QUIT;

}


EXECUTE FUNCTION(Rtv/Add Record        RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009184;
PARAMETER(PAR.Company_Number);
PARAMETER(PAR.Order_Number);
PARAMETER(PAR.ED_Export_Document_Type);
PARAMETER(PAR.Country_Code);
PARAMETER(LCL.EDO_Comment_Line_No);
PARAMETER('CNOCHG');
PARAMETER(PAR.ED_Consignor_Name_PREV);
PARAMETER(PAR.ED_Consignor_Name);
PARAMETER('PNMOCH');
PARAMETER(PAR.*PROGRAM);
{
 //?USER: Processing if Data record not found

 //?Previous (prior , or before image)
 EXECUTE FUNCTION(Rtv Next Line NumSeq  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009185;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 {
  //?USER: Processing if Data record not found

  // PAR.EDO Comment Line Seq = CON.1
  PAR.EDO_Comment_Line_Seq = 1;

  //?USER: Process Data record

  // PAR.EDO Comment Line Seq = DB1.EDO Comment Line Seq + CON.001
  PAR.EDO_Comment_Line_Seq = DB1.EDO_Comment_Line_Seq + 001;

 }

 EXECUTE FUNCTION(Crt Export Doc Order  CR) TYPE(CRTOBJ) FILE(PNGTCPP)           AC2009163;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 PARAMETER(PAR.Prv_EDO_Change_Type);
 PARAMETER(PAR.Prev_Edo_Comment_Text);
 PARAMETER(PAR.Program_Name);
 {
  //?USER: Processing before Data update

  EXECUTE FUNCTION(Set Crt Date/Time PASPGIF) TYPE(EXCINTFUN)                     AC2010529;
  PARAMETER(DB1.Record_Status);
  PARAMETER(DB1.Create_Date);
  PARAMETER(DB1.Create_Time);
  PARAMETER(DB1.Create_User);
  PARAMETER(DB1.Create_Program);
  PARAMETER(DB1.Change_Date);
  PARAMETER(DB1.Change_Time);
  PARAMETER(DB1.Change_User);
  PARAMETER(DB1.Change_Program);
  PARAMETER(PAR.Program_Name);
  {
   //?Execute internal function

   //?*** Set RECORD STATUS field. ***
   // PAR.Record Status = CND.Active
   PAR.Record_Status = 'A';

   //?*** Set CREATE fields. ***
   // PAR.Create Date = JOB.*Job date
   PAR.Create_Date = JOB.*Job_date;

   // PAR.Create Time = JOB.*Job time
   PAR.Create_Time = JOB.*Job_time;

   // PAR.Create User = JOB.*USER
   PAR.Create_User = JOB.*USER;

   //?Update the program name
   // PAR.Create Program = PAR.Program Name
   PAR.Create_Program = PAR.Program_Name;

   //?*** Set CHANGE fields. ***
   // PAR.Change Date = CON.*ZERO
   PAR.Change_Date = *ZERO;

   // PAR.Change Time = CON.*ZERO
   PAR.Change_Time = *ZERO;

   // PAR.Change User = CON.*BLANK
   PAR.Change_User = *BLANK;

   // PAR.Change Program = CON.*BLANK
   PAR.Change_Program = *BLANK;

  }

 }

 //?Current   (new or current information)
 EXECUTE FUNCTION(Rtv Next Line NumSeq  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009185;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 {
  //?USER: Processing if Data record not found

  // PAR.EDO Comment Line Seq = CON.1
  PAR.EDO_Comment_Line_Seq = 1;

  //?USER: Process Data record

  // PAR.EDO Comment Line Seq = DB1.EDO Comment Line Seq + CON.001
  PAR.EDO_Comment_Line_Seq = DB1.EDO_Comment_Line_Seq + 001;

 }

 EXECUTE FUNCTION(Crt Export Doc Order  CR) TYPE(CRTOBJ) FILE(PNGTCPP)           AC2009163;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 PARAMETER(PAR.EDO_Change_Type);
 PARAMETER(PAR.Curr_Edo_Comment_Text);
 PARAMETER(PAR.Program_Name);
 {
  //?USER: Processing before Data update

  EXECUTE FUNCTION(Set Crt Date/Time PASPGIF) TYPE(EXCINTFUN)                     AC2010529;
  PARAMETER(DB1.Record_Status);
  PARAMETER(DB1.Create_Date);
  PARAMETER(DB1.Create_Time);
  PARAMETER(DB1.Create_User);
  PARAMETER(DB1.Create_Program);
  PARAMETER(DB1.Change_Date);
  PARAMETER(DB1.Change_Time);
  PARAMETER(DB1.Change_User);
  PARAMETER(DB1.Change_Program);
  PARAMETER(PAR.Program_Name);
  {
   //?Execute internal function

   //?*** Set RECORD STATUS field. ***
   // PAR.Record Status = CND.Active
   PAR.Record_Status = 'A';

   //?*** Set CREATE fields. ***
   // PAR.Create Date = JOB.*Job date
   PAR.Create_Date = JOB.*Job_date;

   // PAR.Create Time = JOB.*Job time
   PAR.Create_Time = JOB.*Job_time;

   // PAR.Create User = JOB.*USER
   PAR.Create_User = JOB.*USER;

   //?Update the program name
   // PAR.Create Program = PAR.Program Name
   PAR.Create_Program = PAR.Program_Name;

   //?*** Set CHANGE fields. ***
   // PAR.Change Date = CON.*ZERO
   PAR.Change_Date = *ZERO;

   // PAR.Change Time = CON.*ZERO
   PAR.Change_Time = *ZERO;

   // PAR.Change User = CON.*BLANK
   PAR.Change_User = *BLANK;

   // PAR.Change Program = CON.*BLANK
   PAR.Change_Program = *BLANK;

  }

 }

}


ENDIF;

//?ED Consignor Address 1
CASE;

// IF PAR.ED Consignor Addr 1 PREV NE PAR.ED Consignor Addr 1
IF PAR.ED_Consignor_Addr_1_PREV <> PAR.ED_Consignor_Addr_1;

EXECUTE FUNCTION(Rtv Next Line Number  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009181;
PARAMETER(PAR.Company_Number);
PARAMETER(PAR.Order_Number);
PARAMETER(PAR.ED_Export_Document_Type);
PARAMETER(PAR.Country_Code);
PARAMETER(LCL.EDO_Comment_Line_No);
{
 //?USER: Processing if Data record not found

 // PAR.EDO Comment Line No = PAR.EDO Comment Line No + CON.001
 PAR.EDO_Comment_Line_No = PAR.EDO_Comment_Line_No + 001;

 //?USER: Process Data record

 // PAR.EDO Comment Line No = DB1.EDO Comment Line No + CON.001
 PAR.EDO_Comment_Line_No = DB1.EDO_Comment_Line_No + 001;

 QUIT;

}


EXECUTE FUNCTION(Rtv/Add Record        RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009184;
PARAMETER(PAR.Company_Number);
PARAMETER(PAR.Order_Number);
PARAMETER(PAR.ED_Export_Document_Type);
PARAMETER(PAR.Country_Code);
PARAMETER(LCL.EDO_Comment_Line_No);
PARAMETER('CO0001');
PARAMETER(PAR.ED_Consignor_Addr_1_PREV);
PARAMETER(PAR.ED_Consignor_Addr_1);
PARAMETER('PO0001');
PARAMETER(PAR.*PROGRAM);
{
 //?USER: Processing if Data record not found

 //?Previous (prior , or before image)
 EXECUTE FUNCTION(Rtv Next Line NumSeq  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009185;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 {
  //?USER: Processing if Data record not found

  // PAR.EDO Comment Line Seq = CON.1
  PAR.EDO_Comment_Line_Seq = 1;

  //?USER: Process Data record

  // PAR.EDO Comment Line Seq = DB1.EDO Comment Line Seq + CON.001
  PAR.EDO_Comment_Line_Seq = DB1.EDO_Comment_Line_Seq + 001;

 }

 EXECUTE FUNCTION(Crt Export Doc Order  CR) TYPE(CRTOBJ) FILE(PNGTCPP)           AC2009163;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 PARAMETER(PAR.Prv_EDO_Change_Type);
 PARAMETER(PAR.Prev_Edo_Comment_Text);
 PARAMETER(PAR.Program_Name);
 {
  //?USER: Processing before Data update

  EXECUTE FUNCTION(Set Crt Date/Time PASPGIF) TYPE(EXCINTFUN)                     AC2010529;
  PARAMETER(DB1.Record_Status);
  PARAMETER(DB1.Create_Date);
  PARAMETER(DB1.Create_Time);
  PARAMETER(DB1.Create_User);
  PARAMETER(DB1.Create_Program);
  PARAMETER(DB1.Change_Date);
  PARAMETER(DB1.Change_Time);
  PARAMETER(DB1.Change_User);
  PARAMETER(DB1.Change_Program);
  PARAMETER(PAR.Program_Name);
  {
   //?Execute internal function

   //?*** Set RECORD STATUS field. ***
   // PAR.Record Status = CND.Active
   PAR.Record_Status = 'A';

   //?*** Set CREATE fields. ***
   // PAR.Create Date = JOB.*Job date
   PAR.Create_Date = JOB.*Job_date;

   // PAR.Create Time = JOB.*Job time
   PAR.Create_Time = JOB.*Job_time;

   // PAR.Create User = JOB.*USER
   PAR.Create_User = JOB.*USER;

   //?Update the program name
   // PAR.Create Program = PAR.Program Name
   PAR.Create_Program = PAR.Program_Name;

   //?*** Set CHANGE fields. ***
   // PAR.Change Date = CON.*ZERO
   PAR.Change_Date = *ZERO;

   // PAR.Change Time = CON.*ZERO
   PAR.Change_Time = *ZERO;

   // PAR.Change User = CON.*BLANK
   PAR.Change_User = *BLANK;

   // PAR.Change Program = CON.*BLANK
   PAR.Change_Program = *BLANK;

  }

 }

 //?Current   (new or current information)
 EXECUTE FUNCTION(Rtv Next Line NumSeq  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009185;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 {
  //?USER: Processing if Data record not found

  // PAR.EDO Comment Line Seq = CON.1
  PAR.EDO_Comment_Line_Seq = 1;

  //?USER: Process Data record

  // PAR.EDO Comment Line Seq = DB1.EDO Comment Line Seq + CON.001
  PAR.EDO_Comment_Line_Seq = DB1.EDO_Comment_Line_Seq + 001;

 }

 EXECUTE FUNCTION(Crt Export Doc Order  CR) TYPE(CRTOBJ) FILE(PNGTCPP)           AC2009163;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 PARAMETER(PAR.EDO_Change_Type);
 PARAMETER(PAR.Curr_Edo_Comment_Text);
 PARAMETER(PAR.Program_Name);
 {
  //?USER: Processing before Data update

  EXECUTE FUNCTION(Set Crt Date/Time PASPGIF) TYPE(EXCINTFUN)                     AC2010529;
  PARAMETER(DB1.Record_Status);
  PARAMETER(DB1.Create_Date);
  PARAMETER(DB1.Create_Time);
  PARAMETER(DB1.Create_User);
  PARAMETER(DB1.Create_Program);
  PARAMETER(DB1.Change_Date);
  PARAMETER(DB1.Change_Time);
  PARAMETER(DB1.Change_User);
  PARAMETER(DB1.Change_Program);
  PARAMETER(PAR.Program_Name);
  {
   //?Execute internal function

   //?*** Set RECORD STATUS field. ***
   // PAR.Record Status = CND.Active
   PAR.Record_Status = 'A';

   //?*** Set CREATE fields. ***
   // PAR.Create Date = JOB.*Job date
   PAR.Create_Date = JOB.*Job_date;

   // PAR.Create Time = JOB.*Job time
   PAR.Create_Time = JOB.*Job_time;

   // PAR.Create User = JOB.*USER
   PAR.Create_User = JOB.*USER;

   //?Update the program name
   // PAR.Create Program = PAR.Program Name
   PAR.Create_Program = PAR.Program_Name;

   //?*** Set CHANGE fields. ***
   // PAR.Change Date = CON.*ZERO
   PAR.Change_Date = *ZERO;

   // PAR.Change Time = CON.*ZERO
   PAR.Change_Time = *ZERO;

   // PAR.Change User = CON.*BLANK
   PAR.Change_User = *BLANK;

   // PAR.Change Program = CON.*BLANK
   PAR.Change_Program = *BLANK;

  }

 }

}


ENDIF;

//?ED Consignor Address 2
CASE;

// IF PAR.ED Consignor Addr 2 PREV NE PAR.ED Consignor Addr 2
IF PAR.ED_Consignor_Addr_2_PREV <> PAR.ED_Consignor_Addr_2;

EXECUTE FUNCTION(Rtv Next Line Number  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009181;
PARAMETER(PAR.Company_Number);
PARAMETER(PAR.Order_Number);
PARAMETER(PAR.ED_Export_Document_Type);
PARAMETER(PAR.Country_Code);
PARAMETER(LCL.EDO_Comment_Line_No);
{
 //?USER: Processing if Data record not found

 // PAR.EDO Comment Line No = PAR.EDO Comment Line No + CON.001
 PAR.EDO_Comment_Line_No = PAR.EDO_Comment_Line_No + 001;

 //?USER: Process Data record

 // PAR.EDO Comment Line No = DB1.EDO Comment Line No + CON.001
 PAR.EDO_Comment_Line_No = DB1.EDO_Comment_Line_No + 001;

 QUIT;

}


EXECUTE FUNCTION(Rtv/Add Record        RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009184;
PARAMETER(PAR.Company_Number);
PARAMETER(PAR.Order_Number);
PARAMETER(PAR.ED_Export_Document_Type);
PARAMETER(PAR.Country_Code);
PARAMETER(LCL.EDO_Comment_Line_No);
PARAMETER('CO0002');
PARAMETER(PAR.ED_Consignor_Addr_2_PREV);
PARAMETER(PAR.ED_Consignor_Addr_2);
PARAMETER('PO0002');
PARAMETER(PAR.*PROGRAM);
{
 //?USER: Processing if Data record not found

 //?Previous (prior , or before image)
 EXECUTE FUNCTION(Rtv Next Line NumSeq  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009185;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 {
  //?USER: Processing if Data record not found

  // PAR.EDO Comment Line Seq = CON.1
  PAR.EDO_Comment_Line_Seq = 1;

  //?USER: Process Data record

  // PAR.EDO Comment Line Seq = DB1.EDO Comment Line Seq + CON.001
  PAR.EDO_Comment_Line_Seq = DB1.EDO_Comment_Line_Seq + 001;

 }

 EXECUTE FUNCTION(Crt Export Doc Order  CR) TYPE(CRTOBJ) FILE(PNGTCPP)           AC2009163;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 PARAMETER(PAR.Prv_EDO_Change_Type);
 PARAMETER(PAR.Prev_Edo_Comment_Text);
 PARAMETER(PAR.Program_Name);
 {
  //?USER: Processing before Data update

  EXECUTE FUNCTION(Set Crt Date/Time PASPGIF) TYPE(EXCINTFUN)                     AC2010529;
  PARAMETER(DB1.Record_Status);
  PARAMETER(DB1.Create_Date);
  PARAMETER(DB1.Create_Time);
  PARAMETER(DB1.Create_User);
  PARAMETER(DB1.Create_Program);
  PARAMETER(DB1.Change_Date);
  PARAMETER(DB1.Change_Time);
  PARAMETER(DB1.Change_User);
  PARAMETER(DB1.Change_Program);
  PARAMETER(PAR.Program_Name);
  {
   //?Execute internal function

   //?*** Set RECORD STATUS field. ***
   // PAR.Record Status = CND.Active
   PAR.Record_Status = 'A';

   //?*** Set CREATE fields. ***
   // PAR.Create Date = JOB.*Job date
   PAR.Create_Date = JOB.*Job_date;

   // PAR.Create Time = JOB.*Job time
   PAR.Create_Time = JOB.*Job_time;

   // PAR.Create User = JOB.*USER
   PAR.Create_User = JOB.*USER;

   //?Update the program name
   // PAR.Create Program = PAR.Program Name
   PAR.Create_Program = PAR.Program_Name;

   //?*** Set CHANGE fields. ***
   // PAR.Change Date = CON.*ZERO
   PAR.Change_Date = *ZERO;

   // PAR.Change Time = CON.*ZERO
   PAR.Change_Time = *ZERO;

   // PAR.Change User = CON.*BLANK
   PAR.Change_User = *BLANK;

   // PAR.Change Program = CON.*BLANK
   PAR.Change_Program = *BLANK;

  }

 }

 //?Current   (new or current information)
 EXECUTE FUNCTION(Rtv Next Line NumSeq  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009185;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 {
  //?USER: Processing if Data record not found

  // PAR.EDO Comment Line Seq = CON.1
  PAR.EDO_Comment_Line_Seq = 1;

  //?USER: Process Data record

  // PAR.EDO Comment Line Seq = DB1.EDO Comment Line Seq + CON.001
  PAR.EDO_Comment_Line_Seq = DB1.EDO_Comment_Line_Seq + 001;

 }

 EXECUTE FUNCTION(Crt Export Doc Order  CR) TYPE(CRTOBJ) FILE(PNGTCPP)           AC2009163;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 PARAMETER(PAR.EDO_Change_Type);
 PARAMETER(PAR.Curr_Edo_Comment_Text);
 PARAMETER(PAR.Program_Name);
 {
  //?USER: Processing before Data update

  EXECUTE FUNCTION(Set Crt Date/Time PASPGIF) TYPE(EXCINTFUN)                     AC2010529;
  PARAMETER(DB1.Record_Status);
  PARAMETER(DB1.Create_Date);
  PARAMETER(DB1.Create_Time);
  PARAMETER(DB1.Create_User);
  PARAMETER(DB1.Create_Program);
  PARAMETER(DB1.Change_Date);
  PARAMETER(DB1.Change_Time);
  PARAMETER(DB1.Change_User);
  PARAMETER(DB1.Change_Program);
  PARAMETER(PAR.Program_Name);
  {
   //?Execute internal function

   //?*** Set RECORD STATUS field. ***
   // PAR.Record Status = CND.Active
   PAR.Record_Status = 'A';

   //?*** Set CREATE fields. ***
   // PAR.Create Date = JOB.*Job date
   PAR.Create_Date = JOB.*Job_date;

   // PAR.Create Time = JOB.*Job time
   PAR.Create_Time = JOB.*Job_time;

   // PAR.Create User = JOB.*USER
   PAR.Create_User = JOB.*USER;

   //?Update the program name
   // PAR.Create Program = PAR.Program Name
   PAR.Create_Program = PAR.Program_Name;

   //?*** Set CHANGE fields. ***
   // PAR.Change Date = CON.*ZERO
   PAR.Change_Date = *ZERO;

   // PAR.Change Time = CON.*ZERO
   PAR.Change_Time = *ZERO;

   // PAR.Change User = CON.*BLANK
   PAR.Change_User = *BLANK;

   // PAR.Change Program = CON.*BLANK
   PAR.Change_Program = *BLANK;

  }

 }

}


ENDIF;

//?ED Consignor Address 3
CASE;

// IF PAR.ED Consignor Addr 3 PREV NE PAR.ED Consignor Addr 3
IF PAR.ED_Consignor_Addr_3_PREV <> PAR.ED_Consignor_Addr_3;

EXECUTE FUNCTION(Rtv Next Line Number  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009181;
PARAMETER(PAR.Company_Number);
PARAMETER(PAR.Order_Number);
PARAMETER(PAR.ED_Export_Document_Type);
PARAMETER(PAR.Country_Code);
PARAMETER(LCL.EDO_Comment_Line_No);
{
 //?USER: Processing if Data record not found

 // PAR.EDO Comment Line No = PAR.EDO Comment Line No + CON.001
 PAR.EDO_Comment_Line_No = PAR.EDO_Comment_Line_No + 001;

 //?USER: Process Data record

 // PAR.EDO Comment Line No = DB1.EDO Comment Line No + CON.001
 PAR.EDO_Comment_Line_No = DB1.EDO_Comment_Line_No + 001;

 QUIT;

}


EXECUTE FUNCTION(Rtv/Add Record        RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009184;
PARAMETER(PAR.Company_Number);
PARAMETER(PAR.Order_Number);
PARAMETER(PAR.ED_Export_Document_Type);
PARAMETER(PAR.Country_Code);
PARAMETER(LCL.EDO_Comment_Line_No);
PARAMETER('CO0003');
PARAMETER(PAR.ED_Consignor_Addr_3_PREV);
PARAMETER(PAR.ED_Consignor_Addr_3);
PARAMETER('PO0003');
PARAMETER(PAR.*PROGRAM);
{
 //?USER: Processing if Data record not found

 //?Previous (prior , or before image)
 EXECUTE FUNCTION(Rtv Next Line NumSeq  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009185;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 {
  //?USER: Processing if Data record not found

  // PAR.EDO Comment Line Seq = CON.1
  PAR.EDO_Comment_Line_Seq = 1;

  //?USER: Process Data record

  // PAR.EDO Comment Line Seq = DB1.EDO Comment Line Seq + CON.001
  PAR.EDO_Comment_Line_Seq = DB1.EDO_Comment_Line_Seq + 001;

 }

 EXECUTE FUNCTION(Crt Export Doc Order  CR) TYPE(CRTOBJ) FILE(PNGTCPP)           AC2009163;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 PARAMETER(PAR.Prv_EDO_Change_Type);
 PARAMETER(PAR.Prev_Edo_Comment_Text);
 PARAMETER(PAR.Program_Name);
 {
  //?USER: Processing before Data update

  EXECUTE FUNCTION(Set Crt Date/Time PASPGIF) TYPE(EXCINTFUN)                     AC2010529;
  PARAMETER(DB1.Record_Status);
  PARAMETER(DB1.Create_Date);
  PARAMETER(DB1.Create_Time);
  PARAMETER(DB1.Create_User);
  PARAMETER(DB1.Create_Program);
  PARAMETER(DB1.Change_Date);
  PARAMETER(DB1.Change_Time);
  PARAMETER(DB1.Change_User);
  PARAMETER(DB1.Change_Program);
  PARAMETER(PAR.Program_Name);
  {
   //?Execute internal function

   //?*** Set RECORD STATUS field. ***
   // PAR.Record Status = CND.Active
   PAR.Record_Status = 'A';

   //?*** Set CREATE fields. ***
   // PAR.Create Date = JOB.*Job date
   PAR.Create_Date = JOB.*Job_date;

   // PAR.Create Time = JOB.*Job time
   PAR.Create_Time = JOB.*Job_time;

   // PAR.Create User = JOB.*USER
   PAR.Create_User = JOB.*USER;

   //?Update the program name
   // PAR.Create Program = PAR.Program Name
   PAR.Create_Program = PAR.Program_Name;

   //?*** Set CHANGE fields. ***
   // PAR.Change Date = CON.*ZERO
   PAR.Change_Date = *ZERO;

   // PAR.Change Time = CON.*ZERO
   PAR.Change_Time = *ZERO;

   // PAR.Change User = CON.*BLANK
   PAR.Change_User = *BLANK;

   // PAR.Change Program = CON.*BLANK
   PAR.Change_Program = *BLANK;

  }

 }

 //?Current   (new or current information)
 EXECUTE FUNCTION(Rtv Next Line NumSeq  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009185;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 {
  //?USER: Processing if Data record not found

  // PAR.EDO Comment Line Seq = CON.1
  PAR.EDO_Comment_Line_Seq = 1;

  //?USER: Process Data record

  // PAR.EDO Comment Line Seq = DB1.EDO Comment Line Seq + CON.001
  PAR.EDO_Comment_Line_Seq = DB1.EDO_Comment_Line_Seq + 001;

 }

 EXECUTE FUNCTION(Crt Export Doc Order  CR) TYPE(CRTOBJ) FILE(PNGTCPP)           AC2009163;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 PARAMETER(PAR.EDO_Change_Type);
 PARAMETER(PAR.Curr_Edo_Comment_Text);
 PARAMETER(PAR.Program_Name);
 {
  //?USER: Processing before Data update

  EXECUTE FUNCTION(Set Crt Date/Time PASPGIF) TYPE(EXCINTFUN)                     AC2010529;
  PARAMETER(DB1.Record_Status);
  PARAMETER(DB1.Create_Date);
  PARAMETER(DB1.Create_Time);
  PARAMETER(DB1.Create_User);
  PARAMETER(DB1.Create_Program);
  PARAMETER(DB1.Change_Date);
  PARAMETER(DB1.Change_Time);
  PARAMETER(DB1.Change_User);
  PARAMETER(DB1.Change_Program);
  PARAMETER(PAR.Program_Name);
  {
   //?Execute internal function

   //?*** Set RECORD STATUS field. ***
   // PAR.Record Status = CND.Active
   PAR.Record_Status = 'A';

   //?*** Set CREATE fields. ***
   // PAR.Create Date = JOB.*Job date
   PAR.Create_Date = JOB.*Job_date;

   // PAR.Create Time = JOB.*Job time
   PAR.Create_Time = JOB.*Job_time;

   // PAR.Create User = JOB.*USER
   PAR.Create_User = JOB.*USER;

   //?Update the program name
   // PAR.Create Program = PAR.Program Name
   PAR.Create_Program = PAR.Program_Name;

   //?*** Set CHANGE fields. ***
   // PAR.Change Date = CON.*ZERO
   PAR.Change_Date = *ZERO;

   // PAR.Change Time = CON.*ZERO
   PAR.Change_Time = *ZERO;

   // PAR.Change User = CON.*BLANK
   PAR.Change_User = *BLANK;

   // PAR.Change Program = CON.*BLANK
   PAR.Change_Program = *BLANK;

  }

 }

}


ENDIF;

//?ED Consignor Address 4                      09/17/2020 DN R16637
CASE;

// IF PAR.ED Consignor Addr 4 PREV NE PAR.ED Consignor Addr 4
IF PAR.ED_Consignor_Addr_4_PREV <> PAR.ED_Consignor_Addr_4;

EXECUTE FUNCTION(Rtv Next Line Number  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009181;
PARAMETER(PAR.Company_Number);
PARAMETER(PAR.Order_Number);
PARAMETER(PAR.ED_Export_Document_Type);
PARAMETER(PAR.Country_Code);
PARAMETER(LCL.EDO_Comment_Line_No);
{
 //?USER: Processing if Data record not found

 // PAR.EDO Comment Line No = PAR.EDO Comment Line No + CON.001
 PAR.EDO_Comment_Line_No = PAR.EDO_Comment_Line_No + 001;

 //?USER: Process Data record

 // PAR.EDO Comment Line No = DB1.EDO Comment Line No + CON.001
 PAR.EDO_Comment_Line_No = DB1.EDO_Comment_Line_No + 001;

 QUIT;

}


EXECUTE FUNCTION(Rtv/Add Record        RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009184;
PARAMETER(PAR.Company_Number);
PARAMETER(PAR.Order_Number);
PARAMETER(PAR.ED_Export_Document_Type);
PARAMETER(PAR.Country_Code);
PARAMETER(LCL.EDO_Comment_Line_No);
PARAMETER('CO0004');
PARAMETER(PAR.ED_Consignor_Addr_4_PREV);
PARAMETER(PAR.ED_Consignor_Addr_4);
PARAMETER('PO0004');
PARAMETER(PAR.*PROGRAM);
{
 //?USER: Processing if Data record not found

 //?Previous (prior , or before image)
 EXECUTE FUNCTION(Rtv Next Line NumSeq  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009185;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 {
  //?USER: Processing if Data record not found

  // PAR.EDO Comment Line Seq = CON.1
  PAR.EDO_Comment_Line_Seq = 1;

  //?USER: Process Data record

  // PAR.EDO Comment Line Seq = DB1.EDO Comment Line Seq + CON.001
  PAR.EDO_Comment_Line_Seq = DB1.EDO_Comment_Line_Seq + 001;

 }

 EXECUTE FUNCTION(Crt Export Doc Order  CR) TYPE(CRTOBJ) FILE(PNGTCPP)           AC2009163;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 PARAMETER(PAR.Prv_EDO_Change_Type);
 PARAMETER(PAR.Prev_Edo_Comment_Text);
 PARAMETER(PAR.Program_Name);
 {
  //?USER: Processing before Data update

  EXECUTE FUNCTION(Set Crt Date/Time PASPGIF) TYPE(EXCINTFUN)                     AC2010529;
  PARAMETER(DB1.Record_Status);
  PARAMETER(DB1.Create_Date);
  PARAMETER(DB1.Create_Time);
  PARAMETER(DB1.Create_User);
  PARAMETER(DB1.Create_Program);
  PARAMETER(DB1.Change_Date);
  PARAMETER(DB1.Change_Time);
  PARAMETER(DB1.Change_User);
  PARAMETER(DB1.Change_Program);
  PARAMETER(PAR.Program_Name);
  {
   //?Execute internal function

   //?*** Set RECORD STATUS field. ***
   // PAR.Record Status = CND.Active
   PAR.Record_Status = 'A';

   //?*** Set CREATE fields. ***
   // PAR.Create Date = JOB.*Job date
   PAR.Create_Date = JOB.*Job_date;

   // PAR.Create Time = JOB.*Job time
   PAR.Create_Time = JOB.*Job_time;

   // PAR.Create User = JOB.*USER
   PAR.Create_User = JOB.*USER;

   //?Update the program name
   // PAR.Create Program = PAR.Program Name
   PAR.Create_Program = PAR.Program_Name;

   //?*** Set CHANGE fields. ***
   // PAR.Change Date = CON.*ZERO
   PAR.Change_Date = *ZERO;

   // PAR.Change Time = CON.*ZERO
   PAR.Change_Time = *ZERO;

   // PAR.Change User = CON.*BLANK
   PAR.Change_User = *BLANK;

   // PAR.Change Program = CON.*BLANK
   PAR.Change_Program = *BLANK;

  }

 }

 //?Current   (new or current information)
 EXECUTE FUNCTION(Rtv Next Line NumSeq  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009185;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 {
  //?USER: Processing if Data record not found

  // PAR.EDO Comment Line Seq = CON.1
  PAR.EDO_Comment_Line_Seq = 1;

  //?USER: Process Data record

  // PAR.EDO Comment Line Seq = DB1.EDO Comment Line Seq + CON.001
  PAR.EDO_Comment_Line_Seq = DB1.EDO_Comment_Line_Seq + 001;

 }

 EXECUTE FUNCTION(Crt Export Doc Order  CR) TYPE(CRTOBJ) FILE(PNGTCPP)           AC2009163;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 PARAMETER(PAR.EDO_Change_Type);
 PARAMETER(PAR.Curr_Edo_Comment_Text);
 PARAMETER(PAR.Program_Name);
 {
  //?USER: Processing before Data update

  EXECUTE FUNCTION(Set Crt Date/Time PASPGIF) TYPE(EXCINTFUN)                     AC2010529;
  PARAMETER(DB1.Record_Status);
  PARAMETER(DB1.Create_Date);
  PARAMETER(DB1.Create_Time);
  PARAMETER(DB1.Create_User);
  PARAMETER(DB1.Create_Program);
  PARAMETER(DB1.Change_Date);
  PARAMETER(DB1.Change_Time);
  PARAMETER(DB1.Change_User);
  PARAMETER(DB1.Change_Program);
  PARAMETER(PAR.Program_Name);
  {
   //?Execute internal function

   //?*** Set RECORD STATUS field. ***
   // PAR.Record Status = CND.Active
   PAR.Record_Status = 'A';

   //?*** Set CREATE fields. ***
   // PAR.Create Date = JOB.*Job date
   PAR.Create_Date = JOB.*Job_date;

   // PAR.Create Time = JOB.*Job time
   PAR.Create_Time = JOB.*Job_time;

   // PAR.Create User = JOB.*USER
   PAR.Create_User = JOB.*USER;

   //?Update the program name
   // PAR.Create Program = PAR.Program Name
   PAR.Create_Program = PAR.Program_Name;

   //?*** Set CHANGE fields. ***
   // PAR.Change Date = CON.*ZERO
   PAR.Change_Date = *ZERO;

   // PAR.Change Time = CON.*ZERO
   PAR.Change_Time = *ZERO;

   // PAR.Change User = CON.*BLANK
   PAR.Change_User = *BLANK;

   // PAR.Change Program = CON.*BLANK
   PAR.Change_Program = *BLANK;

  }

 }

}


ENDIF;

//?ED Consignor State                          09/10/2020 DN R16637
CASE;

// IF PAR.ED Consignor State PREV NE PAR.ED Consignor State
IF PAR.ED_Consignor_State_PREV <> PAR.ED_Consignor_State;

EXECUTE FUNCTION(Rtv Next Line Number  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009181;
PARAMETER(PAR.Company_Number);
PARAMETER(PAR.Order_Number);
PARAMETER(PAR.ED_Export_Document_Type);
PARAMETER(PAR.Country_Code);
PARAMETER(LCL.EDO_Comment_Line_No);
{
 //?USER: Processing if Data record not found

 // PAR.EDO Comment Line No = PAR.EDO Comment Line No + CON.001
 PAR.EDO_Comment_Line_No = PAR.EDO_Comment_Line_No + 001;

 //?USER: Process Data record

 // PAR.EDO Comment Line No = DB1.EDO Comment Line No + CON.001
 PAR.EDO_Comment_Line_No = DB1.EDO_Comment_Line_No + 001;

 QUIT;

}


EXECUTE FUNCTION(Rtv/Add Record        RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009184;
PARAMETER(PAR.Company_Number);
PARAMETER(PAR.Order_Number);
PARAMETER(PAR.ED_Export_Document_Type);
PARAMETER(PAR.Country_Code);
PARAMETER(LCL.EDO_Comment_Line_No);
PARAMETER('CO0005');
PARAMETER(PAR.ED_Consignor_State_PREV);
PARAMETER(PAR.ED_Consignor_State);
PARAMETER('PO0005');
PARAMETER(PAR.*PROGRAM);
{
 //?USER: Processing if Data record not found

 //?Previous (prior , or before image)
 EXECUTE FUNCTION(Rtv Next Line NumSeq  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009185;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 {
  //?USER: Processing if Data record not found

  // PAR.EDO Comment Line Seq = CON.1
  PAR.EDO_Comment_Line_Seq = 1;

  //?USER: Process Data record

  // PAR.EDO Comment Line Seq = DB1.EDO Comment Line Seq + CON.001
  PAR.EDO_Comment_Line_Seq = DB1.EDO_Comment_Line_Seq + 001;

 }

 EXECUTE FUNCTION(Crt Export Doc Order  CR) TYPE(CRTOBJ) FILE(PNGTCPP)           AC2009163;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 PARAMETER(PAR.Prv_EDO_Change_Type);
 PARAMETER(PAR.Prev_Edo_Comment_Text);
 PARAMETER(PAR.Program_Name);
 {
  //?USER: Processing before Data update

  EXECUTE FUNCTION(Set Crt Date/Time PASPGIF) TYPE(EXCINTFUN)                     AC2010529;
  PARAMETER(DB1.Record_Status);
  PARAMETER(DB1.Create_Date);
  PARAMETER(DB1.Create_Time);
  PARAMETER(DB1.Create_User);
  PARAMETER(DB1.Create_Program);
  PARAMETER(DB1.Change_Date);
  PARAMETER(DB1.Change_Time);
  PARAMETER(DB1.Change_User);
  PARAMETER(DB1.Change_Program);
  PARAMETER(PAR.Program_Name);
  {
   //?Execute internal function

   //?*** Set RECORD STATUS field. ***
   // PAR.Record Status = CND.Active
   PAR.Record_Status = 'A';

   //?*** Set CREATE fields. ***
   // PAR.Create Date = JOB.*Job date
   PAR.Create_Date = JOB.*Job_date;

   // PAR.Create Time = JOB.*Job time
   PAR.Create_Time = JOB.*Job_time;

   // PAR.Create User = JOB.*USER
   PAR.Create_User = JOB.*USER;

   //?Update the program name
   // PAR.Create Program = PAR.Program Name
   PAR.Create_Program = PAR.Program_Name;

   //?*** Set CHANGE fields. ***
   // PAR.Change Date = CON.*ZERO
   PAR.Change_Date = *ZERO;

   // PAR.Change Time = CON.*ZERO
   PAR.Change_Time = *ZERO;

   // PAR.Change User = CON.*BLANK
   PAR.Change_User = *BLANK;

   // PAR.Change Program = CON.*BLANK
   PAR.Change_Program = *BLANK;

  }

 }

 //?Current   (new or current information)
 EXECUTE FUNCTION(Rtv Next Line NumSeq  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009185;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 {
  //?USER: Processing if Data record not found

  // PAR.EDO Comment Line Seq = CON.1
  PAR.EDO_Comment_Line_Seq = 1;

  //?USER: Process Data record

  // PAR.EDO Comment Line Seq = DB1.EDO Comment Line Seq + CON.001
  PAR.EDO_Comment_Line_Seq = DB1.EDO_Comment_Line_Seq + 001;

 }

 EXECUTE FUNCTION(Crt Export Doc Order  CR) TYPE(CRTOBJ) FILE(PNGTCPP)           AC2009163;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 PARAMETER(PAR.EDO_Change_Type);
 PARAMETER(PAR.Curr_Edo_Comment_Text);
 PARAMETER(PAR.Program_Name);
 {
  //?USER: Processing before Data update

  EXECUTE FUNCTION(Set Crt Date/Time PASPGIF) TYPE(EXCINTFUN)                     AC2010529;
  PARAMETER(DB1.Record_Status);
  PARAMETER(DB1.Create_Date);
  PARAMETER(DB1.Create_Time);
  PARAMETER(DB1.Create_User);
  PARAMETER(DB1.Create_Program);
  PARAMETER(DB1.Change_Date);
  PARAMETER(DB1.Change_Time);
  PARAMETER(DB1.Change_User);
  PARAMETER(DB1.Change_Program);
  PARAMETER(PAR.Program_Name);
  {
   //?Execute internal function

   //?*** Set RECORD STATUS field. ***
   // PAR.Record Status = CND.Active
   PAR.Record_Status = 'A';

   //?*** Set CREATE fields. ***
   // PAR.Create Date = JOB.*Job date
   PAR.Create_Date = JOB.*Job_date;

   // PAR.Create Time = JOB.*Job time
   PAR.Create_Time = JOB.*Job_time;

   // PAR.Create User = JOB.*USER
   PAR.Create_User = JOB.*USER;

   //?Update the program name
   // PAR.Create Program = PAR.Program Name
   PAR.Create_Program = PAR.Program_Name;

   //?*** Set CHANGE fields. ***
   // PAR.Change Date = CON.*ZERO
   PAR.Change_Date = *ZERO;

   // PAR.Change Time = CON.*ZERO
   PAR.Change_Time = *ZERO;

   // PAR.Change User = CON.*BLANK
   PAR.Change_User = *BLANK;

   // PAR.Change Program = CON.*BLANK
   PAR.Change_Program = *BLANK;

  }

 }

}


ENDIF;

//?ED Consignor Postal                         09/10/2020 DN R16637
CASE;

// IF PAR.ED Consignor Postal PREV NE PAR.ED Consignor Postal
IF PAR.ED_Consignor_Postal_PREV <> PAR.ED_Consignor_Postal;

EXECUTE FUNCTION(Rtv Next Line Number  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009181;
PARAMETER(PAR.Company_Number);
PARAMETER(PAR.Order_Number);
PARAMETER(PAR.ED_Export_Document_Type);
PARAMETER(PAR.Country_Code);
PARAMETER(LCL.EDO_Comment_Line_No);
{
 //?USER: Processing if Data record not found

 // PAR.EDO Comment Line No = PAR.EDO Comment Line No + CON.001
 PAR.EDO_Comment_Line_No = PAR.EDO_Comment_Line_No + 001;

 //?USER: Process Data record

 // PAR.EDO Comment Line No = DB1.EDO Comment Line No + CON.001
 PAR.EDO_Comment_Line_No = DB1.EDO_Comment_Line_No + 001;

 QUIT;

}


EXECUTE FUNCTION(Rtv/Add Record        RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009184;
PARAMETER(PAR.Company_Number);
PARAMETER(PAR.Order_Number);
PARAMETER(PAR.ED_Export_Document_Type);
PARAMETER(PAR.Country_Code);
PARAMETER(LCL.EDO_Comment_Line_No);
PARAMETER('CO0006');
PARAMETER(PAR.ED_Consignor_Postal_PREV);
PARAMETER(PAR.ED_Consignor_Postal);
PARAMETER('PO0006');
PARAMETER(PAR.*PROGRAM);
{
 //?USER: Processing if Data record not found

 //?Previous (prior , or before image)
 EXECUTE FUNCTION(Rtv Next Line NumSeq  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009185;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 {
  //?USER: Processing if Data record not found

  // PAR.EDO Comment Line Seq = CON.1
  PAR.EDO_Comment_Line_Seq = 1;

  //?USER: Process Data record

  // PAR.EDO Comment Line Seq = DB1.EDO Comment Line Seq + CON.001
  PAR.EDO_Comment_Line_Seq = DB1.EDO_Comment_Line_Seq + 001;

 }

 EXECUTE FUNCTION(Crt Export Doc Order  CR) TYPE(CRTOBJ) FILE(PNGTCPP)           AC2009163;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 PARAMETER(PAR.Prv_EDO_Change_Type);
 PARAMETER(PAR.Prev_Edo_Comment_Text);
 PARAMETER(PAR.Program_Name);
 {
  //?USER: Processing before Data update

  EXECUTE FUNCTION(Set Crt Date/Time PASPGIF) TYPE(EXCINTFUN)                     AC2010529;
  PARAMETER(DB1.Record_Status);
  PARAMETER(DB1.Create_Date);
  PARAMETER(DB1.Create_Time);
  PARAMETER(DB1.Create_User);
  PARAMETER(DB1.Create_Program);
  PARAMETER(DB1.Change_Date);
  PARAMETER(DB1.Change_Time);
  PARAMETER(DB1.Change_User);
  PARAMETER(DB1.Change_Program);
  PARAMETER(PAR.Program_Name);
  {
   //?Execute internal function

   //?*** Set RECORD STATUS field. ***
   // PAR.Record Status = CND.Active
   PAR.Record_Status = 'A';

   //?*** Set CREATE fields. ***
   // PAR.Create Date = JOB.*Job date
   PAR.Create_Date = JOB.*Job_date;

   // PAR.Create Time = JOB.*Job time
   PAR.Create_Time = JOB.*Job_time;

   // PAR.Create User = JOB.*USER
   PAR.Create_User = JOB.*USER;

   //?Update the program name
   // PAR.Create Program = PAR.Program Name
   PAR.Create_Program = PAR.Program_Name;

   //?*** Set CHANGE fields. ***
   // PAR.Change Date = CON.*ZERO
   PAR.Change_Date = *ZERO;

   // PAR.Change Time = CON.*ZERO
   PAR.Change_Time = *ZERO;

   // PAR.Change User = CON.*BLANK
   PAR.Change_User = *BLANK;

   // PAR.Change Program = CON.*BLANK
   PAR.Change_Program = *BLANK;

  }

 }

 //?Current   (new or current information)
 EXECUTE FUNCTION(Rtv Next Line NumSeq  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009185;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 {
  //?USER: Processing if Data record not found

  // PAR.EDO Comment Line Seq = CON.1
  PAR.EDO_Comment_Line_Seq = 1;

  //?USER: Process Data record

  // PAR.EDO Comment Line Seq = DB1.EDO Comment Line Seq + CON.001
  PAR.EDO_Comment_Line_Seq = DB1.EDO_Comment_Line_Seq + 001;

 }

 EXECUTE FUNCTION(Crt Export Doc Order  CR) TYPE(CRTOBJ) FILE(PNGTCPP)           AC2009163;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 PARAMETER(PAR.EDO_Change_Type);
 PARAMETER(PAR.Curr_Edo_Comment_Text);
 PARAMETER(PAR.Program_Name);
 {
  //?USER: Processing before Data update

  EXECUTE FUNCTION(Set Crt Date/Time PASPGIF) TYPE(EXCINTFUN)                     AC2010529;
  PARAMETER(DB1.Record_Status);
  PARAMETER(DB1.Create_Date);
  PARAMETER(DB1.Create_Time);
  PARAMETER(DB1.Create_User);
  PARAMETER(DB1.Create_Program);
  PARAMETER(DB1.Change_Date);
  PARAMETER(DB1.Change_Time);
  PARAMETER(DB1.Change_User);
  PARAMETER(DB1.Change_Program);
  PARAMETER(PAR.Program_Name);
  {
   //?Execute internal function

   //?*** Set RECORD STATUS field. ***
   // PAR.Record Status = CND.Active
   PAR.Record_Status = 'A';

   //?*** Set CREATE fields. ***
   // PAR.Create Date = JOB.*Job date
   PAR.Create_Date = JOB.*Job_date;

   // PAR.Create Time = JOB.*Job time
   PAR.Create_Time = JOB.*Job_time;

   // PAR.Create User = JOB.*USER
   PAR.Create_User = JOB.*USER;

   //?Update the program name
   // PAR.Create Program = PAR.Program Name
   PAR.Create_Program = PAR.Program_Name;

   //?*** Set CHANGE fields. ***
   // PAR.Change Date = CON.*ZERO
   PAR.Change_Date = *ZERO;

   // PAR.Change Time = CON.*ZERO
   PAR.Change_Time = *ZERO;

   // PAR.Change User = CON.*BLANK
   PAR.Change_User = *BLANK;

   // PAR.Change Program = CON.*BLANK
   PAR.Change_Program = *BLANK;

  }

 }

}


ENDIF;

//?ED Consignor Country                        09/10/2020 DN R16637
CASE;

// IF PAR.ED Consignor Country PREV NE PAR.ED Consignor Country Code
IF PAR.ED_Consignor_Country_PREV <> PAR.ED_Consignor_Country_Code;

EXECUTE FUNCTION(Rtv Next Line Number  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009181;
PARAMETER(PAR.Company_Number);
PARAMETER(PAR.Order_Number);
PARAMETER(PAR.ED_Export_Document_Type);
PARAMETER(PAR.Country_Code);
PARAMETER(LCL.EDO_Comment_Line_No);
{
 //?USER: Processing if Data record not found

 // PAR.EDO Comment Line No = PAR.EDO Comment Line No + CON.001
 PAR.EDO_Comment_Line_No = PAR.EDO_Comment_Line_No + 001;

 //?USER: Process Data record

 // PAR.EDO Comment Line No = DB1.EDO Comment Line No + CON.001
 PAR.EDO_Comment_Line_No = DB1.EDO_Comment_Line_No + 001;

 QUIT;

}


EXECUTE FUNCTION(Rtv/Add Record        RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009184;
PARAMETER(PAR.Company_Number);
PARAMETER(PAR.Order_Number);
PARAMETER(PAR.ED_Export_Document_Type);
PARAMETER(PAR.Country_Code);
PARAMETER(LCL.EDO_Comment_Line_No);
PARAMETER('CO0007');
PARAMETER(PAR.ED_Consignor_Country_PREV);
PARAMETER(PAR.ED_Consignor_Country_Code);
PARAMETER('PO0008');
PARAMETER(PAR.*PROGRAM);
{
 //?USER: Processing if Data record not found

 //?Previous (prior , or before image)
 EXECUTE FUNCTION(Rtv Next Line NumSeq  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009185;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 {
  //?USER: Processing if Data record not found

  // PAR.EDO Comment Line Seq = CON.1
  PAR.EDO_Comment_Line_Seq = 1;

  //?USER: Process Data record

  // PAR.EDO Comment Line Seq = DB1.EDO Comment Line Seq + CON.001
  PAR.EDO_Comment_Line_Seq = DB1.EDO_Comment_Line_Seq + 001;

 }

 EXECUTE FUNCTION(Crt Export Doc Order  CR) TYPE(CRTOBJ) FILE(PNGTCPP)           AC2009163;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 PARAMETER(PAR.Prv_EDO_Change_Type);
 PARAMETER(PAR.Prev_Edo_Comment_Text);
 PARAMETER(PAR.Program_Name);
 {
  //?USER: Processing before Data update

  EXECUTE FUNCTION(Set Crt Date/Time PASPGIF) TYPE(EXCINTFUN)                     AC2010529;
  PARAMETER(DB1.Record_Status);
  PARAMETER(DB1.Create_Date);
  PARAMETER(DB1.Create_Time);
  PARAMETER(DB1.Create_User);
  PARAMETER(DB1.Create_Program);
  PARAMETER(DB1.Change_Date);
  PARAMETER(DB1.Change_Time);
  PARAMETER(DB1.Change_User);
  PARAMETER(DB1.Change_Program);
  PARAMETER(PAR.Program_Name);
  {
   //?Execute internal function

   //?*** Set RECORD STATUS field. ***
   // PAR.Record Status = CND.Active
   PAR.Record_Status = 'A';

   //?*** Set CREATE fields. ***
   // PAR.Create Date = JOB.*Job date
   PAR.Create_Date = JOB.*Job_date;

   // PAR.Create Time = JOB.*Job time
   PAR.Create_Time = JOB.*Job_time;

   // PAR.Create User = JOB.*USER
   PAR.Create_User = JOB.*USER;

   //?Update the program name
   // PAR.Create Program = PAR.Program Name
   PAR.Create_Program = PAR.Program_Name;

   //?*** Set CHANGE fields. ***
   // PAR.Change Date = CON.*ZERO
   PAR.Change_Date = *ZERO;

   // PAR.Change Time = CON.*ZERO
   PAR.Change_Time = *ZERO;

   // PAR.Change User = CON.*BLANK
   PAR.Change_User = *BLANK;

   // PAR.Change Program = CON.*BLANK
   PAR.Change_Program = *BLANK;

  }

 }

 //?Current   (new or current information)
 EXECUTE FUNCTION(Rtv Next Line NumSeq  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009185;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 {
  //?USER: Processing if Data record not found

  // PAR.EDO Comment Line Seq = CON.1
  PAR.EDO_Comment_Line_Seq = 1;

  //?USER: Process Data record

  // PAR.EDO Comment Line Seq = DB1.EDO Comment Line Seq + CON.001
  PAR.EDO_Comment_Line_Seq = DB1.EDO_Comment_Line_Seq + 001;

 }

 EXECUTE FUNCTION(Crt Export Doc Order  CR) TYPE(CRTOBJ) FILE(PNGTCPP)           AC2009163;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 PARAMETER(PAR.EDO_Change_Type);
 PARAMETER(PAR.Curr_Edo_Comment_Text);
 PARAMETER(PAR.Program_Name);
 {
  //?USER: Processing before Data update

  EXECUTE FUNCTION(Set Crt Date/Time PASPGIF) TYPE(EXCINTFUN)                     AC2010529;
  PARAMETER(DB1.Record_Status);
  PARAMETER(DB1.Create_Date);
  PARAMETER(DB1.Create_Time);
  PARAMETER(DB1.Create_User);
  PARAMETER(DB1.Create_Program);
  PARAMETER(DB1.Change_Date);
  PARAMETER(DB1.Change_Time);
  PARAMETER(DB1.Change_User);
  PARAMETER(DB1.Change_Program);
  PARAMETER(PAR.Program_Name);
  {
   //?Execute internal function

   //?*** Set RECORD STATUS field. ***
   // PAR.Record Status = CND.Active
   PAR.Record_Status = 'A';

   //?*** Set CREATE fields. ***
   // PAR.Create Date = JOB.*Job date
   PAR.Create_Date = JOB.*Job_date;

   // PAR.Create Time = JOB.*Job time
   PAR.Create_Time = JOB.*Job_time;

   // PAR.Create User = JOB.*USER
   PAR.Create_User = JOB.*USER;

   //?Update the program name
   // PAR.Create Program = PAR.Program Name
   PAR.Create_Program = PAR.Program_Name;

   //?*** Set CHANGE fields. ***
   // PAR.Change Date = CON.*ZERO
   PAR.Change_Date = *ZERO;

   // PAR.Change Time = CON.*ZERO
   PAR.Change_Time = *ZERO;

   // PAR.Change User = CON.*BLANK
   PAR.Change_User = *BLANK;

   // PAR.Change Program = CON.*BLANK
   PAR.Change_Program = *BLANK;

  }

 }

}


ENDIF;

//?ED ConsignEE Name
CASE;

// IF PAR.ED Consignee Name PREV NE PAR.ED Consignee Name
IF PAR.ED_Consignee_Name_PREV <> PAR.ED_Consignee_Name;

EXECUTE FUNCTION(Rtv Next Line Number  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009181;
PARAMETER(PAR.Company_Number);
PARAMETER(PAR.Order_Number);
PARAMETER(PAR.ED_Export_Document_Type);
PARAMETER(PAR.Country_Code);
PARAMETER(LCL.EDO_Comment_Line_No);
{
 //?USER: Processing if Data record not found

 // PAR.EDO Comment Line No = PAR.EDO Comment Line No + CON.001
 PAR.EDO_Comment_Line_No = PAR.EDO_Comment_Line_No + 001;

 //?USER: Process Data record

 // PAR.EDO Comment Line No = DB1.EDO Comment Line No + CON.001
 PAR.EDO_Comment_Line_No = DB1.EDO_Comment_Line_No + 001;

 QUIT;

}


EXECUTE FUNCTION(Rtv/Add Record        RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009184;
PARAMETER(PAR.Company_Number);
PARAMETER(PAR.Order_Number);
PARAMETER(PAR.ED_Export_Document_Type);
PARAMETER(PAR.Country_Code);
PARAMETER(LCL.EDO_Comment_Line_No);
PARAMETER('CNECHG');
PARAMETER(PAR.ED_Consignee_Name_PREV);
PARAMETER(PAR.ED_Consignee_Name);
PARAMETER('PNMECH');
PARAMETER(PAR.*PROGRAM);
{
 //?USER: Processing if Data record not found

 //?Previous (prior , or before image)
 EXECUTE FUNCTION(Rtv Next Line NumSeq  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009185;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 {
  //?USER: Processing if Data record not found

  // PAR.EDO Comment Line Seq = CON.1
  PAR.EDO_Comment_Line_Seq = 1;

  //?USER: Process Data record

  // PAR.EDO Comment Line Seq = DB1.EDO Comment Line Seq + CON.001
  PAR.EDO_Comment_Line_Seq = DB1.EDO_Comment_Line_Seq + 001;

 }

 EXECUTE FUNCTION(Crt Export Doc Order  CR) TYPE(CRTOBJ) FILE(PNGTCPP)           AC2009163;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 PARAMETER(PAR.Prv_EDO_Change_Type);
 PARAMETER(PAR.Prev_Edo_Comment_Text);
 PARAMETER(PAR.Program_Name);
 {
  //?USER: Processing before Data update

  EXECUTE FUNCTION(Set Crt Date/Time PASPGIF) TYPE(EXCINTFUN)                     AC2010529;
  PARAMETER(DB1.Record_Status);
  PARAMETER(DB1.Create_Date);
  PARAMETER(DB1.Create_Time);
  PARAMETER(DB1.Create_User);
  PARAMETER(DB1.Create_Program);
  PARAMETER(DB1.Change_Date);
  PARAMETER(DB1.Change_Time);
  PARAMETER(DB1.Change_User);
  PARAMETER(DB1.Change_Program);
  PARAMETER(PAR.Program_Name);
  {
   //?Execute internal function

   //?*** Set RECORD STATUS field. ***
   // PAR.Record Status = CND.Active
   PAR.Record_Status = 'A';

   //?*** Set CREATE fields. ***
   // PAR.Create Date = JOB.*Job date
   PAR.Create_Date = JOB.*Job_date;

   // PAR.Create Time = JOB.*Job time
   PAR.Create_Time = JOB.*Job_time;

   // PAR.Create User = JOB.*USER
   PAR.Create_User = JOB.*USER;

   //?Update the program name
   // PAR.Create Program = PAR.Program Name
   PAR.Create_Program = PAR.Program_Name;

   //?*** Set CHANGE fields. ***
   // PAR.Change Date = CON.*ZERO
   PAR.Change_Date = *ZERO;

   // PAR.Change Time = CON.*ZERO
   PAR.Change_Time = *ZERO;

   // PAR.Change User = CON.*BLANK
   PAR.Change_User = *BLANK;

   // PAR.Change Program = CON.*BLANK
   PAR.Change_Program = *BLANK;

  }

 }

 //?Current   (new or current information)
 EXECUTE FUNCTION(Rtv Next Line NumSeq  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009185;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 {
  //?USER: Processing if Data record not found

  // PAR.EDO Comment Line Seq = CON.1
  PAR.EDO_Comment_Line_Seq = 1;

  //?USER: Process Data record

  // PAR.EDO Comment Line Seq = DB1.EDO Comment Line Seq + CON.001
  PAR.EDO_Comment_Line_Seq = DB1.EDO_Comment_Line_Seq + 001;

 }

 EXECUTE FUNCTION(Crt Export Doc Order  CR) TYPE(CRTOBJ) FILE(PNGTCPP)           AC2009163;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 PARAMETER(PAR.EDO_Change_Type);
 PARAMETER(PAR.Curr_Edo_Comment_Text);
 PARAMETER(PAR.Program_Name);
 {
  //?USER: Processing before Data update

  EXECUTE FUNCTION(Set Crt Date/Time PASPGIF) TYPE(EXCINTFUN)                     AC2010529;
  PARAMETER(DB1.Record_Status);
  PARAMETER(DB1.Create_Date);
  PARAMETER(DB1.Create_Time);
  PARAMETER(DB1.Create_User);
  PARAMETER(DB1.Create_Program);
  PARAMETER(DB1.Change_Date);
  PARAMETER(DB1.Change_Time);
  PARAMETER(DB1.Change_User);
  PARAMETER(DB1.Change_Program);
  PARAMETER(PAR.Program_Name);
  {
   //?Execute internal function

   //?*** Set RECORD STATUS field. ***
   // PAR.Record Status = CND.Active
   PAR.Record_Status = 'A';

   //?*** Set CREATE fields. ***
   // PAR.Create Date = JOB.*Job date
   PAR.Create_Date = JOB.*Job_date;

   // PAR.Create Time = JOB.*Job time
   PAR.Create_Time = JOB.*Job_time;

   // PAR.Create User = JOB.*USER
   PAR.Create_User = JOB.*USER;

   //?Update the program name
   // PAR.Create Program = PAR.Program Name
   PAR.Create_Program = PAR.Program_Name;

   //?*** Set CHANGE fields. ***
   // PAR.Change Date = CON.*ZERO
   PAR.Change_Date = *ZERO;

   // PAR.Change Time = CON.*ZERO
   PAR.Change_Time = *ZERO;

   // PAR.Change User = CON.*BLANK
   PAR.Change_User = *BLANK;

   // PAR.Change Program = CON.*BLANK
   PAR.Change_Program = *BLANK;

  }

 }

}


ENDIF;

//?ED ConsignEE Address 1
CASE;

// IF PAR.ED Consignee Addr 1 PREV NE PAR.ED Consignee Addr 1
IF PAR.ED_Consignee_Addr_1_PREV <> PAR.ED_Consignee_Addr_1;

EXECUTE FUNCTION(Rtv Next Line Number  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009181;
PARAMETER(PAR.Company_Number);
PARAMETER(PAR.Order_Number);
PARAMETER(PAR.ED_Export_Document_Type);
PARAMETER(PAR.Country_Code);
PARAMETER(LCL.EDO_Comment_Line_No);
{
 //?USER: Processing if Data record not found

 // PAR.EDO Comment Line No = PAR.EDO Comment Line No + CON.001
 PAR.EDO_Comment_Line_No = PAR.EDO_Comment_Line_No + 001;

 //?USER: Process Data record

 // PAR.EDO Comment Line No = DB1.EDO Comment Line No + CON.001
 PAR.EDO_Comment_Line_No = DB1.EDO_Comment_Line_No + 001;

 QUIT;

}


EXECUTE FUNCTION(Rtv/Add Record        RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009184;
PARAMETER(PAR.Company_Number);
PARAMETER(PAR.Order_Number);
PARAMETER(PAR.ED_Export_Document_Type);
PARAMETER(PAR.Country_Code);
PARAMETER(LCL.EDO_Comment_Line_No);
PARAMETER('CE0001');
PARAMETER(PAR.ED_Consignee_Addr_1_PREV);
PARAMETER(PAR.ED_Consignee_Addr_1);
PARAMETER('PE0001');
PARAMETER(PAR.*PROGRAM);
{
 //?USER: Processing if Data record not found

 //?Previous (prior , or before image)
 EXECUTE FUNCTION(Rtv Next Line NumSeq  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009185;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 {
  //?USER: Processing if Data record not found

  // PAR.EDO Comment Line Seq = CON.1
  PAR.EDO_Comment_Line_Seq = 1;

  //?USER: Process Data record

  // PAR.EDO Comment Line Seq = DB1.EDO Comment Line Seq + CON.001
  PAR.EDO_Comment_Line_Seq = DB1.EDO_Comment_Line_Seq + 001;

 }

 EXECUTE FUNCTION(Crt Export Doc Order  CR) TYPE(CRTOBJ) FILE(PNGTCPP)           AC2009163;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 PARAMETER(PAR.Prv_EDO_Change_Type);
 PARAMETER(PAR.Prev_Edo_Comment_Text);
 PARAMETER(PAR.Program_Name);
 {
  //?USER: Processing before Data update

  EXECUTE FUNCTION(Set Crt Date/Time PASPGIF) TYPE(EXCINTFUN)                     AC2010529;
  PARAMETER(DB1.Record_Status);
  PARAMETER(DB1.Create_Date);
  PARAMETER(DB1.Create_Time);
  PARAMETER(DB1.Create_User);
  PARAMETER(DB1.Create_Program);
  PARAMETER(DB1.Change_Date);
  PARAMETER(DB1.Change_Time);
  PARAMETER(DB1.Change_User);
  PARAMETER(DB1.Change_Program);
  PARAMETER(PAR.Program_Name);
  {
   //?Execute internal function

   //?*** Set RECORD STATUS field. ***
   // PAR.Record Status = CND.Active
   PAR.Record_Status = 'A';

   //?*** Set CREATE fields. ***
   // PAR.Create Date = JOB.*Job date
   PAR.Create_Date = JOB.*Job_date;

   // PAR.Create Time = JOB.*Job time
   PAR.Create_Time = JOB.*Job_time;

   // PAR.Create User = JOB.*USER
   PAR.Create_User = JOB.*USER;

   //?Update the program name
   // PAR.Create Program = PAR.Program Name
   PAR.Create_Program = PAR.Program_Name;

   //?*** Set CHANGE fields. ***
   // PAR.Change Date = CON.*ZERO
   PAR.Change_Date = *ZERO;

   // PAR.Change Time = CON.*ZERO
   PAR.Change_Time = *ZERO;

   // PAR.Change User = CON.*BLANK
   PAR.Change_User = *BLANK;

   // PAR.Change Program = CON.*BLANK
   PAR.Change_Program = *BLANK;

  }

 }

 //?Current   (new or current information)
 EXECUTE FUNCTION(Rtv Next Line NumSeq  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009185;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 {
  //?USER: Processing if Data record not found

  // PAR.EDO Comment Line Seq = CON.1
  PAR.EDO_Comment_Line_Seq = 1;

  //?USER: Process Data record

  // PAR.EDO Comment Line Seq = DB1.EDO Comment Line Seq + CON.001
  PAR.EDO_Comment_Line_Seq = DB1.EDO_Comment_Line_Seq + 001;

 }

 EXECUTE FUNCTION(Crt Export Doc Order  CR) TYPE(CRTOBJ) FILE(PNGTCPP)           AC2009163;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 PARAMETER(PAR.EDO_Change_Type);
 PARAMETER(PAR.Curr_Edo_Comment_Text);
 PARAMETER(PAR.Program_Name);
 {
  //?USER: Processing before Data update

  EXECUTE FUNCTION(Set Crt Date/Time PASPGIF) TYPE(EXCINTFUN)                     AC2010529;
  PARAMETER(DB1.Record_Status);
  PARAMETER(DB1.Create_Date);
  PARAMETER(DB1.Create_Time);
  PARAMETER(DB1.Create_User);
  PARAMETER(DB1.Create_Program);
  PARAMETER(DB1.Change_Date);
  PARAMETER(DB1.Change_Time);
  PARAMETER(DB1.Change_User);
  PARAMETER(DB1.Change_Program);
  PARAMETER(PAR.Program_Name);
  {
   //?Execute internal function

   //?*** Set RECORD STATUS field. ***
   // PAR.Record Status = CND.Active
   PAR.Record_Status = 'A';

   //?*** Set CREATE fields. ***
   // PAR.Create Date = JOB.*Job date
   PAR.Create_Date = JOB.*Job_date;

   // PAR.Create Time = JOB.*Job time
   PAR.Create_Time = JOB.*Job_time;

   // PAR.Create User = JOB.*USER
   PAR.Create_User = JOB.*USER;

   //?Update the program name
   // PAR.Create Program = PAR.Program Name
   PAR.Create_Program = PAR.Program_Name;

   //?*** Set CHANGE fields. ***
   // PAR.Change Date = CON.*ZERO
   PAR.Change_Date = *ZERO;

   // PAR.Change Time = CON.*ZERO
   PAR.Change_Time = *ZERO;

   // PAR.Change User = CON.*BLANK
   PAR.Change_User = *BLANK;

   // PAR.Change Program = CON.*BLANK
   PAR.Change_Program = *BLANK;

  }

 }

}


ENDIF;

//?ED ConsignEE Address 2
CASE;

// IF PAR.ED Consignee Addr 2 PREV NE PAR.ED Consignee Addr 2
IF PAR.ED_Consignee_Addr_2_PREV <> PAR.ED_Consignee_Addr_2;

EXECUTE FUNCTION(Rtv Next Line Number  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009181;
PARAMETER(PAR.Company_Number);
PARAMETER(PAR.Order_Number);
PARAMETER(PAR.ED_Export_Document_Type);
PARAMETER(PAR.Country_Code);
PARAMETER(LCL.EDO_Comment_Line_No);
{
 //?USER: Processing if Data record not found

 // PAR.EDO Comment Line No = PAR.EDO Comment Line No + CON.001
 PAR.EDO_Comment_Line_No = PAR.EDO_Comment_Line_No + 001;

 //?USER: Process Data record

 // PAR.EDO Comment Line No = DB1.EDO Comment Line No + CON.001
 PAR.EDO_Comment_Line_No = DB1.EDO_Comment_Line_No + 001;

 QUIT;

}


EXECUTE FUNCTION(Rtv/Add Record        RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009184;
PARAMETER(PAR.Company_Number);
PARAMETER(PAR.Order_Number);
PARAMETER(PAR.ED_Export_Document_Type);
PARAMETER(PAR.Country_Code);
PARAMETER(LCL.EDO_Comment_Line_No);
PARAMETER('CE0002');
PARAMETER(PAR.ED_Consignee_Addr_2_PREV);
PARAMETER(PAR.ED_Consignee_Addr_2);
PARAMETER('PE0002');
PARAMETER(PAR.*PROGRAM);
{
 //?USER: Processing if Data record not found

 //?Previous (prior , or before image)
 EXECUTE FUNCTION(Rtv Next Line NumSeq  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009185;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 {
  //?USER: Processing if Data record not found

  // PAR.EDO Comment Line Seq = CON.1
  PAR.EDO_Comment_Line_Seq = 1;

  //?USER: Process Data record

  // PAR.EDO Comment Line Seq = DB1.EDO Comment Line Seq + CON.001
  PAR.EDO_Comment_Line_Seq = DB1.EDO_Comment_Line_Seq + 001;

 }

 EXECUTE FUNCTION(Crt Export Doc Order  CR) TYPE(CRTOBJ) FILE(PNGTCPP)           AC2009163;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 PARAMETER(PAR.Prv_EDO_Change_Type);
 PARAMETER(PAR.Prev_Edo_Comment_Text);
 PARAMETER(PAR.Program_Name);
 {
  //?USER: Processing before Data update

  EXECUTE FUNCTION(Set Crt Date/Time PASPGIF) TYPE(EXCINTFUN)                     AC2010529;
  PARAMETER(DB1.Record_Status);
  PARAMETER(DB1.Create_Date);
  PARAMETER(DB1.Create_Time);
  PARAMETER(DB1.Create_User);
  PARAMETER(DB1.Create_Program);
  PARAMETER(DB1.Change_Date);
  PARAMETER(DB1.Change_Time);
  PARAMETER(DB1.Change_User);
  PARAMETER(DB1.Change_Program);
  PARAMETER(PAR.Program_Name);
  {
   //?Execute internal function

   //?*** Set RECORD STATUS field. ***
   // PAR.Record Status = CND.Active
   PAR.Record_Status = 'A';

   //?*** Set CREATE fields. ***
   // PAR.Create Date = JOB.*Job date
   PAR.Create_Date = JOB.*Job_date;

   // PAR.Create Time = JOB.*Job time
   PAR.Create_Time = JOB.*Job_time;

   // PAR.Create User = JOB.*USER
   PAR.Create_User = JOB.*USER;

   //?Update the program name
   // PAR.Create Program = PAR.Program Name
   PAR.Create_Program = PAR.Program_Name;

   //?*** Set CHANGE fields. ***
   // PAR.Change Date = CON.*ZERO
   PAR.Change_Date = *ZERO;

   // PAR.Change Time = CON.*ZERO
   PAR.Change_Time = *ZERO;

   // PAR.Change User = CON.*BLANK
   PAR.Change_User = *BLANK;

   // PAR.Change Program = CON.*BLANK
   PAR.Change_Program = *BLANK;

  }

 }

 //?Current   (new or current information)
 EXECUTE FUNCTION(Rtv Next Line NumSeq  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009185;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 {
  //?USER: Processing if Data record not found

  // PAR.EDO Comment Line Seq = CON.1
  PAR.EDO_Comment_Line_Seq = 1;

  //?USER: Process Data record

  // PAR.EDO Comment Line Seq = DB1.EDO Comment Line Seq + CON.001
  PAR.EDO_Comment_Line_Seq = DB1.EDO_Comment_Line_Seq + 001;

 }

 EXECUTE FUNCTION(Crt Export Doc Order  CR) TYPE(CRTOBJ) FILE(PNGTCPP)           AC2009163;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 PARAMETER(PAR.EDO_Change_Type);
 PARAMETER(PAR.Curr_Edo_Comment_Text);
 PARAMETER(PAR.Program_Name);
 {
  //?USER: Processing before Data update

  EXECUTE FUNCTION(Set Crt Date/Time PASPGIF) TYPE(EXCINTFUN)                     AC2010529;
  PARAMETER(DB1.Record_Status);
  PARAMETER(DB1.Create_Date);
  PARAMETER(DB1.Create_Time);
  PARAMETER(DB1.Create_User);
  PARAMETER(DB1.Create_Program);
  PARAMETER(DB1.Change_Date);
  PARAMETER(DB1.Change_Time);
  PARAMETER(DB1.Change_User);
  PARAMETER(DB1.Change_Program);
  PARAMETER(PAR.Program_Name);
  {
   //?Execute internal function

   //?*** Set RECORD STATUS field. ***
   // PAR.Record Status = CND.Active
   PAR.Record_Status = 'A';

   //?*** Set CREATE fields. ***
   // PAR.Create Date = JOB.*Job date
   PAR.Create_Date = JOB.*Job_date;

   // PAR.Create Time = JOB.*Job time
   PAR.Create_Time = JOB.*Job_time;

   // PAR.Create User = JOB.*USER
   PAR.Create_User = JOB.*USER;

   //?Update the program name
   // PAR.Create Program = PAR.Program Name
   PAR.Create_Program = PAR.Program_Name;

   //?*** Set CHANGE fields. ***
   // PAR.Change Date = CON.*ZERO
   PAR.Change_Date = *ZERO;

   // PAR.Change Time = CON.*ZERO
   PAR.Change_Time = *ZERO;

   // PAR.Change User = CON.*BLANK
   PAR.Change_User = *BLANK;

   // PAR.Change Program = CON.*BLANK
   PAR.Change_Program = *BLANK;

  }

 }

}


ENDIF;

//?ED ConsignEE Address 3
CASE;

// IF PAR.ED Consignee Addr 3 PREV NE PAR.ED Consignee Addr 3
IF PAR.ED_Consignee_Addr_3_PREV <> PAR.ED_Consignee_Addr_3;

EXECUTE FUNCTION(Rtv Next Line Number  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009181;
PARAMETER(PAR.Company_Number);
PARAMETER(PAR.Order_Number);
PARAMETER(PAR.ED_Export_Document_Type);
PARAMETER(PAR.Country_Code);
PARAMETER(LCL.EDO_Comment_Line_No);
{
 //?USER: Processing if Data record not found

 // PAR.EDO Comment Line No = PAR.EDO Comment Line No + CON.001
 PAR.EDO_Comment_Line_No = PAR.EDO_Comment_Line_No + 001;

 //?USER: Process Data record

 // PAR.EDO Comment Line No = DB1.EDO Comment Line No + CON.001
 PAR.EDO_Comment_Line_No = DB1.EDO_Comment_Line_No + 001;

 QUIT;

}


EXECUTE FUNCTION(Rtv/Add Record        RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009184;
PARAMETER(PAR.Company_Number);
PARAMETER(PAR.Order_Number);
PARAMETER(PAR.ED_Export_Document_Type);
PARAMETER(PAR.Country_Code);
PARAMETER(LCL.EDO_Comment_Line_No);
PARAMETER('CE0003');
PARAMETER(PAR.ED_Consignee_Addr_3_PREV);
PARAMETER(PAR.ED_Consignee_Addr_3);
PARAMETER('PE0003');
PARAMETER(PAR.*PROGRAM);
{
 //?USER: Processing if Data record not found

 //?Previous (prior , or before image)
 EXECUTE FUNCTION(Rtv Next Line NumSeq  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009185;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 {
  //?USER: Processing if Data record not found

  // PAR.EDO Comment Line Seq = CON.1
  PAR.EDO_Comment_Line_Seq = 1;

  //?USER: Process Data record

  // PAR.EDO Comment Line Seq = DB1.EDO Comment Line Seq + CON.001
  PAR.EDO_Comment_Line_Seq = DB1.EDO_Comment_Line_Seq + 001;

 }

 EXECUTE FUNCTION(Crt Export Doc Order  CR) TYPE(CRTOBJ) FILE(PNGTCPP)           AC2009163;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 PARAMETER(PAR.Prv_EDO_Change_Type);
 PARAMETER(PAR.Prev_Edo_Comment_Text);
 PARAMETER(PAR.Program_Name);
 {
  //?USER: Processing before Data update

  EXECUTE FUNCTION(Set Crt Date/Time PASPGIF) TYPE(EXCINTFUN)                     AC2010529;
  PARAMETER(DB1.Record_Status);
  PARAMETER(DB1.Create_Date);
  PARAMETER(DB1.Create_Time);
  PARAMETER(DB1.Create_User);
  PARAMETER(DB1.Create_Program);
  PARAMETER(DB1.Change_Date);
  PARAMETER(DB1.Change_Time);
  PARAMETER(DB1.Change_User);
  PARAMETER(DB1.Change_Program);
  PARAMETER(PAR.Program_Name);
  {
   //?Execute internal function

   //?*** Set RECORD STATUS field. ***
   // PAR.Record Status = CND.Active
   PAR.Record_Status = 'A';

   //?*** Set CREATE fields. ***
   // PAR.Create Date = JOB.*Job date
   PAR.Create_Date = JOB.*Job_date;

   // PAR.Create Time = JOB.*Job time
   PAR.Create_Time = JOB.*Job_time;

   // PAR.Create User = JOB.*USER
   PAR.Create_User = JOB.*USER;

   //?Update the program name
   // PAR.Create Program = PAR.Program Name
   PAR.Create_Program = PAR.Program_Name;

   //?*** Set CHANGE fields. ***
   // PAR.Change Date = CON.*ZERO
   PAR.Change_Date = *ZERO;

   // PAR.Change Time = CON.*ZERO
   PAR.Change_Time = *ZERO;

   // PAR.Change User = CON.*BLANK
   PAR.Change_User = *BLANK;

   // PAR.Change Program = CON.*BLANK
   PAR.Change_Program = *BLANK;

  }

 }

 //?Current   (new or current information)
 EXECUTE FUNCTION(Rtv Next Line NumSeq  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009185;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 {
  //?USER: Processing if Data record not found

  // PAR.EDO Comment Line Seq = CON.1
  PAR.EDO_Comment_Line_Seq = 1;

  //?USER: Process Data record

  // PAR.EDO Comment Line Seq = DB1.EDO Comment Line Seq + CON.001
  PAR.EDO_Comment_Line_Seq = DB1.EDO_Comment_Line_Seq + 001;

 }

 EXECUTE FUNCTION(Crt Export Doc Order  CR) TYPE(CRTOBJ) FILE(PNGTCPP)           AC2009163;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 PARAMETER(PAR.EDO_Change_Type);
 PARAMETER(PAR.Curr_Edo_Comment_Text);
 PARAMETER(PAR.Program_Name);
 {
  //?USER: Processing before Data update

  EXECUTE FUNCTION(Set Crt Date/Time PASPGIF) TYPE(EXCINTFUN)                     AC2010529;
  PARAMETER(DB1.Record_Status);
  PARAMETER(DB1.Create_Date);
  PARAMETER(DB1.Create_Time);
  PARAMETER(DB1.Create_User);
  PARAMETER(DB1.Create_Program);
  PARAMETER(DB1.Change_Date);
  PARAMETER(DB1.Change_Time);
  PARAMETER(DB1.Change_User);
  PARAMETER(DB1.Change_Program);
  PARAMETER(PAR.Program_Name);
  {
   //?Execute internal function

   //?*** Set RECORD STATUS field. ***
   // PAR.Record Status = CND.Active
   PAR.Record_Status = 'A';

   //?*** Set CREATE fields. ***
   // PAR.Create Date = JOB.*Job date
   PAR.Create_Date = JOB.*Job_date;

   // PAR.Create Time = JOB.*Job time
   PAR.Create_Time = JOB.*Job_time;

   // PAR.Create User = JOB.*USER
   PAR.Create_User = JOB.*USER;

   //?Update the program name
   // PAR.Create Program = PAR.Program Name
   PAR.Create_Program = PAR.Program_Name;

   //?*** Set CHANGE fields. ***
   // PAR.Change Date = CON.*ZERO
   PAR.Change_Date = *ZERO;

   // PAR.Change Time = CON.*ZERO
   PAR.Change_Time = *ZERO;

   // PAR.Change User = CON.*BLANK
   PAR.Change_User = *BLANK;

   // PAR.Change Program = CON.*BLANK
   PAR.Change_Program = *BLANK;

  }

 }

}


ENDIF;

//?ED ConsignEE Address 4
CASE;

// IF PAR.ED Consignee Addr 4 PREV NE PAR.ED Consignee Addr 4
IF PAR.ED_Consignee_Addr_4_PREV <> PAR.ED_Consignee_Addr_4;

EXECUTE FUNCTION(Rtv Next Line Number  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009181;
PARAMETER(PAR.Company_Number);
PARAMETER(PAR.Order_Number);
PARAMETER(PAR.ED_Export_Document_Type);
PARAMETER(PAR.Country_Code);
PARAMETER(LCL.EDO_Comment_Line_No);
{
 //?USER: Processing if Data record not found

 // PAR.EDO Comment Line No = PAR.EDO Comment Line No + CON.001
 PAR.EDO_Comment_Line_No = PAR.EDO_Comment_Line_No + 001;

 //?USER: Process Data record

 // PAR.EDO Comment Line No = DB1.EDO Comment Line No + CON.001
 PAR.EDO_Comment_Line_No = DB1.EDO_Comment_Line_No + 001;

 QUIT;

}


EXECUTE FUNCTION(Rtv/Add Record        RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009184;
PARAMETER(PAR.Company_Number);
PARAMETER(PAR.Order_Number);
PARAMETER(PAR.ED_Export_Document_Type);
PARAMETER(PAR.Country_Code);
PARAMETER(LCL.EDO_Comment_Line_No);
PARAMETER('CE0004');
PARAMETER(PAR.ED_Consignee_Addr_4_PREV);
PARAMETER(PAR.ED_Consignee_Addr_4);
PARAMETER('PE0004');
PARAMETER(PAR.*PROGRAM);
{
 //?USER: Processing if Data record not found

 //?Previous (prior , or before image)
 EXECUTE FUNCTION(Rtv Next Line NumSeq  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009185;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 {
  //?USER: Processing if Data record not found

  // PAR.EDO Comment Line Seq = CON.1
  PAR.EDO_Comment_Line_Seq = 1;

  //?USER: Process Data record

  // PAR.EDO Comment Line Seq = DB1.EDO Comment Line Seq + CON.001
  PAR.EDO_Comment_Line_Seq = DB1.EDO_Comment_Line_Seq + 001;

 }

 EXECUTE FUNCTION(Crt Export Doc Order  CR) TYPE(CRTOBJ) FILE(PNGTCPP)           AC2009163;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 PARAMETER(PAR.Prv_EDO_Change_Type);
 PARAMETER(PAR.Prev_Edo_Comment_Text);
 PARAMETER(PAR.Program_Name);
 {
  //?USER: Processing before Data update

  EXECUTE FUNCTION(Set Crt Date/Time PASPGIF) TYPE(EXCINTFUN)                     AC2010529;
  PARAMETER(DB1.Record_Status);
  PARAMETER(DB1.Create_Date);
  PARAMETER(DB1.Create_Time);
  PARAMETER(DB1.Create_User);
  PARAMETER(DB1.Create_Program);
  PARAMETER(DB1.Change_Date);
  PARAMETER(DB1.Change_Time);
  PARAMETER(DB1.Change_User);
  PARAMETER(DB1.Change_Program);
  PARAMETER(PAR.Program_Name);
  {
   //?Execute internal function

   //?*** Set RECORD STATUS field. ***
   // PAR.Record Status = CND.Active
   PAR.Record_Status = 'A';

   //?*** Set CREATE fields. ***
   // PAR.Create Date = JOB.*Job date
   PAR.Create_Date = JOB.*Job_date;

   // PAR.Create Time = JOB.*Job time
   PAR.Create_Time = JOB.*Job_time;

   // PAR.Create User = JOB.*USER
   PAR.Create_User = JOB.*USER;

   //?Update the program name
   // PAR.Create Program = PAR.Program Name
   PAR.Create_Program = PAR.Program_Name;

   //?*** Set CHANGE fields. ***
   // PAR.Change Date = CON.*ZERO
   PAR.Change_Date = *ZERO;

   // PAR.Change Time = CON.*ZERO
   PAR.Change_Time = *ZERO;

   // PAR.Change User = CON.*BLANK
   PAR.Change_User = *BLANK;

   // PAR.Change Program = CON.*BLANK
   PAR.Change_Program = *BLANK;

  }

 }

 //?Current   (new or current information)
 EXECUTE FUNCTION(Rtv Next Line NumSeq  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009185;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 {
  //?USER: Processing if Data record not found

  // PAR.EDO Comment Line Seq = CON.1
  PAR.EDO_Comment_Line_Seq = 1;

  //?USER: Process Data record

  // PAR.EDO Comment Line Seq = DB1.EDO Comment Line Seq + CON.001
  PAR.EDO_Comment_Line_Seq = DB1.EDO_Comment_Line_Seq + 001;

 }

 EXECUTE FUNCTION(Crt Export Doc Order  CR) TYPE(CRTOBJ) FILE(PNGTCPP)           AC2009163;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 PARAMETER(PAR.EDO_Change_Type);
 PARAMETER(PAR.Curr_Edo_Comment_Text);
 PARAMETER(PAR.Program_Name);
 {
  //?USER: Processing before Data update

  EXECUTE FUNCTION(Set Crt Date/Time PASPGIF) TYPE(EXCINTFUN)                     AC2010529;
  PARAMETER(DB1.Record_Status);
  PARAMETER(DB1.Create_Date);
  PARAMETER(DB1.Create_Time);
  PARAMETER(DB1.Create_User);
  PARAMETER(DB1.Create_Program);
  PARAMETER(DB1.Change_Date);
  PARAMETER(DB1.Change_Time);
  PARAMETER(DB1.Change_User);
  PARAMETER(DB1.Change_Program);
  PARAMETER(PAR.Program_Name);
  {
   //?Execute internal function

   //?*** Set RECORD STATUS field. ***
   // PAR.Record Status = CND.Active
   PAR.Record_Status = 'A';

   //?*** Set CREATE fields. ***
   // PAR.Create Date = JOB.*Job date
   PAR.Create_Date = JOB.*Job_date;

   // PAR.Create Time = JOB.*Job time
   PAR.Create_Time = JOB.*Job_time;

   // PAR.Create User = JOB.*USER
   PAR.Create_User = JOB.*USER;

   //?Update the program name
   // PAR.Create Program = PAR.Program Name
   PAR.Create_Program = PAR.Program_Name;

   //?*** Set CHANGE fields. ***
   // PAR.Change Date = CON.*ZERO
   PAR.Change_Date = *ZERO;

   // PAR.Change Time = CON.*ZERO
   PAR.Change_Time = *ZERO;

   // PAR.Change User = CON.*BLANK
   PAR.Change_User = *BLANK;

   // PAR.Change Program = CON.*BLANK
   PAR.Change_Program = *BLANK;

  }

 }

}


ENDIF;

//?ED ConsignEE State                          09/10/2020 DN R16637
CASE;

// IF PAR.ED Consignee State PREV NE PAR.ED Consignee State
IF PAR.ED_Consignee_State_PREV <> PAR.ED_Consignee_State;

EXECUTE FUNCTION(Rtv Next Line Number  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009181;
PARAMETER(PAR.Company_Number);
PARAMETER(PAR.Order_Number);
PARAMETER(PAR.ED_Export_Document_Type);
PARAMETER(PAR.Country_Code);
PARAMETER(LCL.EDO_Comment_Line_No);
{
 //?USER: Processing if Data record not found

 // PAR.EDO Comment Line No = PAR.EDO Comment Line No + CON.001
 PAR.EDO_Comment_Line_No = PAR.EDO_Comment_Line_No + 001;

 //?USER: Process Data record

 // PAR.EDO Comment Line No = DB1.EDO Comment Line No + CON.001
 PAR.EDO_Comment_Line_No = DB1.EDO_Comment_Line_No + 001;

 QUIT;

}


EXECUTE FUNCTION(Rtv/Add Record        RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009184;
PARAMETER(PAR.Company_Number);
PARAMETER(PAR.Order_Number);
PARAMETER(PAR.ED_Export_Document_Type);
PARAMETER(PAR.Country_Code);
PARAMETER(LCL.EDO_Comment_Line_No);
PARAMETER('CE0005');
PARAMETER(PAR.ED_Consignee_State_PREV);
PARAMETER(PAR.ED_Consignee_State);
PARAMETER('PE0005');
PARAMETER(PAR.*PROGRAM);
{
 //?USER: Processing if Data record not found

 //?Previous (prior , or before image)
 EXECUTE FUNCTION(Rtv Next Line NumSeq  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009185;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 {
  //?USER: Processing if Data record not found

  // PAR.EDO Comment Line Seq = CON.1
  PAR.EDO_Comment_Line_Seq = 1;

  //?USER: Process Data record

  // PAR.EDO Comment Line Seq = DB1.EDO Comment Line Seq + CON.001
  PAR.EDO_Comment_Line_Seq = DB1.EDO_Comment_Line_Seq + 001;

 }

 EXECUTE FUNCTION(Crt Export Doc Order  CR) TYPE(CRTOBJ) FILE(PNGTCPP)           AC2009163;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 PARAMETER(PAR.Prv_EDO_Change_Type);
 PARAMETER(PAR.Prev_Edo_Comment_Text);
 PARAMETER(PAR.Program_Name);
 {
  //?USER: Processing before Data update

  EXECUTE FUNCTION(Set Crt Date/Time PASPGIF) TYPE(EXCINTFUN)                     AC2010529;
  PARAMETER(DB1.Record_Status);
  PARAMETER(DB1.Create_Date);
  PARAMETER(DB1.Create_Time);
  PARAMETER(DB1.Create_User);
  PARAMETER(DB1.Create_Program);
  PARAMETER(DB1.Change_Date);
  PARAMETER(DB1.Change_Time);
  PARAMETER(DB1.Change_User);
  PARAMETER(DB1.Change_Program);
  PARAMETER(PAR.Program_Name);
  {
   //?Execute internal function

   //?*** Set RECORD STATUS field. ***
   // PAR.Record Status = CND.Active
   PAR.Record_Status = 'A';

   //?*** Set CREATE fields. ***
   // PAR.Create Date = JOB.*Job date
   PAR.Create_Date = JOB.*Job_date;

   // PAR.Create Time = JOB.*Job time
   PAR.Create_Time = JOB.*Job_time;

   // PAR.Create User = JOB.*USER
   PAR.Create_User = JOB.*USER;

   //?Update the program name
   // PAR.Create Program = PAR.Program Name
   PAR.Create_Program = PAR.Program_Name;

   //?*** Set CHANGE fields. ***
   // PAR.Change Date = CON.*ZERO
   PAR.Change_Date = *ZERO;

   // PAR.Change Time = CON.*ZERO
   PAR.Change_Time = *ZERO;

   // PAR.Change User = CON.*BLANK
   PAR.Change_User = *BLANK;

   // PAR.Change Program = CON.*BLANK
   PAR.Change_Program = *BLANK;

  }

 }

 //?Current   (new or current information)
 EXECUTE FUNCTION(Rtv Next Line NumSeq  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009185;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 {
  //?USER: Processing if Data record not found

  // PAR.EDO Comment Line Seq = CON.1
  PAR.EDO_Comment_Line_Seq = 1;

  //?USER: Process Data record

  // PAR.EDO Comment Line Seq = DB1.EDO Comment Line Seq + CON.001
  PAR.EDO_Comment_Line_Seq = DB1.EDO_Comment_Line_Seq + 001;

 }

 EXECUTE FUNCTION(Crt Export Doc Order  CR) TYPE(CRTOBJ) FILE(PNGTCPP)           AC2009163;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 PARAMETER(PAR.EDO_Change_Type);
 PARAMETER(PAR.Curr_Edo_Comment_Text);
 PARAMETER(PAR.Program_Name);
 {
  //?USER: Processing before Data update

  EXECUTE FUNCTION(Set Crt Date/Time PASPGIF) TYPE(EXCINTFUN)                     AC2010529;
  PARAMETER(DB1.Record_Status);
  PARAMETER(DB1.Create_Date);
  PARAMETER(DB1.Create_Time);
  PARAMETER(DB1.Create_User);
  PARAMETER(DB1.Create_Program);
  PARAMETER(DB1.Change_Date);
  PARAMETER(DB1.Change_Time);
  PARAMETER(DB1.Change_User);
  PARAMETER(DB1.Change_Program);
  PARAMETER(PAR.Program_Name);
  {
   //?Execute internal function

   //?*** Set RECORD STATUS field. ***
   // PAR.Record Status = CND.Active
   PAR.Record_Status = 'A';

   //?*** Set CREATE fields. ***
   // PAR.Create Date = JOB.*Job date
   PAR.Create_Date = JOB.*Job_date;

   // PAR.Create Time = JOB.*Job time
   PAR.Create_Time = JOB.*Job_time;

   // PAR.Create User = JOB.*USER
   PAR.Create_User = JOB.*USER;

   //?Update the program name
   // PAR.Create Program = PAR.Program Name
   PAR.Create_Program = PAR.Program_Name;

   //?*** Set CHANGE fields. ***
   // PAR.Change Date = CON.*ZERO
   PAR.Change_Date = *ZERO;

   // PAR.Change Time = CON.*ZERO
   PAR.Change_Time = *ZERO;

   // PAR.Change User = CON.*BLANK
   PAR.Change_User = *BLANK;

   // PAR.Change Program = CON.*BLANK
   PAR.Change_Program = *BLANK;

  }

 }

}


ENDIF;

//?ED ConsignEE Postal                         09/10/2020 DN R16637
CASE;

// IF PAR.ED Consignee Postal PREV NE PAR.ED Consignee Postal
IF PAR.ED_Consignee_Postal_PREV <> PAR.ED_Consignee_Postal;

EXECUTE FUNCTION(Rtv Next Line Number  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009181;
PARAMETER(PAR.Company_Number);
PARAMETER(PAR.Order_Number);
PARAMETER(PAR.ED_Export_Document_Type);
PARAMETER(PAR.Country_Code);
PARAMETER(LCL.EDO_Comment_Line_No);
{
 //?USER: Processing if Data record not found

 // PAR.EDO Comment Line No = PAR.EDO Comment Line No + CON.001
 PAR.EDO_Comment_Line_No = PAR.EDO_Comment_Line_No + 001;

 //?USER: Process Data record

 // PAR.EDO Comment Line No = DB1.EDO Comment Line No + CON.001
 PAR.EDO_Comment_Line_No = DB1.EDO_Comment_Line_No + 001;

 QUIT;

}


EXECUTE FUNCTION(Rtv/Add Record        RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009184;
PARAMETER(PAR.Company_Number);
PARAMETER(PAR.Order_Number);
PARAMETER(PAR.ED_Export_Document_Type);
PARAMETER(PAR.Country_Code);
PARAMETER(LCL.EDO_Comment_Line_No);
PARAMETER('CE0006');
PARAMETER(PAR.ED_Consignee_Postal_PREV);
PARAMETER(PAR.ED_Consignee_Postal);
PARAMETER('PE0006');
PARAMETER(PAR.*PROGRAM);
{
 //?USER: Processing if Data record not found

 //?Previous (prior , or before image)
 EXECUTE FUNCTION(Rtv Next Line NumSeq  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009185;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 {
  //?USER: Processing if Data record not found

  // PAR.EDO Comment Line Seq = CON.1
  PAR.EDO_Comment_Line_Seq = 1;

  //?USER: Process Data record

  // PAR.EDO Comment Line Seq = DB1.EDO Comment Line Seq + CON.001
  PAR.EDO_Comment_Line_Seq = DB1.EDO_Comment_Line_Seq + 001;

 }

 EXECUTE FUNCTION(Crt Export Doc Order  CR) TYPE(CRTOBJ) FILE(PNGTCPP)           AC2009163;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 PARAMETER(PAR.Prv_EDO_Change_Type);
 PARAMETER(PAR.Prev_Edo_Comment_Text);
 PARAMETER(PAR.Program_Name);
 {
  //?USER: Processing before Data update

  EXECUTE FUNCTION(Set Crt Date/Time PASPGIF) TYPE(EXCINTFUN)                     AC2010529;
  PARAMETER(DB1.Record_Status);
  PARAMETER(DB1.Create_Date);
  PARAMETER(DB1.Create_Time);
  PARAMETER(DB1.Create_User);
  PARAMETER(DB1.Create_Program);
  PARAMETER(DB1.Change_Date);
  PARAMETER(DB1.Change_Time);
  PARAMETER(DB1.Change_User);
  PARAMETER(DB1.Change_Program);
  PARAMETER(PAR.Program_Name);
  {
   //?Execute internal function

   //?*** Set RECORD STATUS field. ***
   // PAR.Record Status = CND.Active
   PAR.Record_Status = 'A';

   //?*** Set CREATE fields. ***
   // PAR.Create Date = JOB.*Job date
   PAR.Create_Date = JOB.*Job_date;

   // PAR.Create Time = JOB.*Job time
   PAR.Create_Time = JOB.*Job_time;

   // PAR.Create User = JOB.*USER
   PAR.Create_User = JOB.*USER;

   //?Update the program name
   // PAR.Create Program = PAR.Program Name
   PAR.Create_Program = PAR.Program_Name;

   //?*** Set CHANGE fields. ***
   // PAR.Change Date = CON.*ZERO
   PAR.Change_Date = *ZERO;

   // PAR.Change Time = CON.*ZERO
   PAR.Change_Time = *ZERO;

   // PAR.Change User = CON.*BLANK
   PAR.Change_User = *BLANK;

   // PAR.Change Program = CON.*BLANK
   PAR.Change_Program = *BLANK;

  }

 }

 //?Current   (new or current information)
 EXECUTE FUNCTION(Rtv Next Line NumSeq  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009185;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 {
  //?USER: Processing if Data record not found

  // PAR.EDO Comment Line Seq = CON.1
  PAR.EDO_Comment_Line_Seq = 1;

  //?USER: Process Data record

  // PAR.EDO Comment Line Seq = DB1.EDO Comment Line Seq + CON.001
  PAR.EDO_Comment_Line_Seq = DB1.EDO_Comment_Line_Seq + 001;

 }

 EXECUTE FUNCTION(Crt Export Doc Order  CR) TYPE(CRTOBJ) FILE(PNGTCPP)           AC2009163;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 PARAMETER(PAR.EDO_Change_Type);
 PARAMETER(PAR.Curr_Edo_Comment_Text);
 PARAMETER(PAR.Program_Name);
 {
  //?USER: Processing before Data update

  EXECUTE FUNCTION(Set Crt Date/Time PASPGIF) TYPE(EXCINTFUN)                     AC2010529;
  PARAMETER(DB1.Record_Status);
  PARAMETER(DB1.Create_Date);
  PARAMETER(DB1.Create_Time);
  PARAMETER(DB1.Create_User);
  PARAMETER(DB1.Create_Program);
  PARAMETER(DB1.Change_Date);
  PARAMETER(DB1.Change_Time);
  PARAMETER(DB1.Change_User);
  PARAMETER(DB1.Change_Program);
  PARAMETER(PAR.Program_Name);
  {
   //?Execute internal function

   //?*** Set RECORD STATUS field. ***
   // PAR.Record Status = CND.Active
   PAR.Record_Status = 'A';

   //?*** Set CREATE fields. ***
   // PAR.Create Date = JOB.*Job date
   PAR.Create_Date = JOB.*Job_date;

   // PAR.Create Time = JOB.*Job time
   PAR.Create_Time = JOB.*Job_time;

   // PAR.Create User = JOB.*USER
   PAR.Create_User = JOB.*USER;

   //?Update the program name
   // PAR.Create Program = PAR.Program Name
   PAR.Create_Program = PAR.Program_Name;

   //?*** Set CHANGE fields. ***
   // PAR.Change Date = CON.*ZERO
   PAR.Change_Date = *ZERO;

   // PAR.Change Time = CON.*ZERO
   PAR.Change_Time = *ZERO;

   // PAR.Change User = CON.*BLANK
   PAR.Change_User = *BLANK;

   // PAR.Change Program = CON.*BLANK
   PAR.Change_Program = *BLANK;

  }

 }

}


ENDIF;

//?ED ConsignEE Country                        09/10/2020 DN R16637
CASE;

// IF PAR.ED Consignee Country PREV NE PAR.ED Consignee Country Code
IF PAR.ED_Consignee_Country_PREV <> PAR.ED_Consignee_Country_Code;

EXECUTE FUNCTION(Rtv Next Line Number  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009181;
PARAMETER(PAR.Company_Number);
PARAMETER(PAR.Order_Number);
PARAMETER(PAR.ED_Export_Document_Type);
PARAMETER(PAR.Country_Code);
PARAMETER(LCL.EDO_Comment_Line_No);
{
 //?USER: Processing if Data record not found

 // PAR.EDO Comment Line No = PAR.EDO Comment Line No + CON.001
 PAR.EDO_Comment_Line_No = PAR.EDO_Comment_Line_No + 001;

 //?USER: Process Data record

 // PAR.EDO Comment Line No = DB1.EDO Comment Line No + CON.001
 PAR.EDO_Comment_Line_No = DB1.EDO_Comment_Line_No + 001;

 QUIT;

}


EXECUTE FUNCTION(Rtv/Add Record        RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009184;
PARAMETER(PAR.Company_Number);
PARAMETER(PAR.Order_Number);
PARAMETER(PAR.ED_Export_Document_Type);
PARAMETER(PAR.Country_Code);
PARAMETER(LCL.EDO_Comment_Line_No);
PARAMETER('CE0007');
PARAMETER(PAR.ED_Consignee_Country_PREV);
PARAMETER(PAR.ED_Consignee_Country_Code);
PARAMETER('PE0008');
PARAMETER(PAR.*PROGRAM);
{
 //?USER: Processing if Data record not found

 //?Previous (prior , or before image)
 EXECUTE FUNCTION(Rtv Next Line NumSeq  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009185;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 {
  //?USER: Processing if Data record not found

  // PAR.EDO Comment Line Seq = CON.1
  PAR.EDO_Comment_Line_Seq = 1;

  //?USER: Process Data record

  // PAR.EDO Comment Line Seq = DB1.EDO Comment Line Seq + CON.001
  PAR.EDO_Comment_Line_Seq = DB1.EDO_Comment_Line_Seq + 001;

 }

 EXECUTE FUNCTION(Crt Export Doc Order  CR) TYPE(CRTOBJ) FILE(PNGTCPP)           AC2009163;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 PARAMETER(PAR.Prv_EDO_Change_Type);
 PARAMETER(PAR.Prev_Edo_Comment_Text);
 PARAMETER(PAR.Program_Name);
 {
  //?USER: Processing before Data update

  EXECUTE FUNCTION(Set Crt Date/Time PASPGIF) TYPE(EXCINTFUN)                     AC2010529;
  PARAMETER(DB1.Record_Status);
  PARAMETER(DB1.Create_Date);
  PARAMETER(DB1.Create_Time);
  PARAMETER(DB1.Create_User);
  PARAMETER(DB1.Create_Program);
  PARAMETER(DB1.Change_Date);
  PARAMETER(DB1.Change_Time);
  PARAMETER(DB1.Change_User);
  PARAMETER(DB1.Change_Program);
  PARAMETER(PAR.Program_Name);
  {
   //?Execute internal function

   //?*** Set RECORD STATUS field. ***
   // PAR.Record Status = CND.Active
   PAR.Record_Status = 'A';

   //?*** Set CREATE fields. ***
   // PAR.Create Date = JOB.*Job date
   PAR.Create_Date = JOB.*Job_date;

   // PAR.Create Time = JOB.*Job time
   PAR.Create_Time = JOB.*Job_time;

   // PAR.Create User = JOB.*USER
   PAR.Create_User = JOB.*USER;

   //?Update the program name
   // PAR.Create Program = PAR.Program Name
   PAR.Create_Program = PAR.Program_Name;

   //?*** Set CHANGE fields. ***
   // PAR.Change Date = CON.*ZERO
   PAR.Change_Date = *ZERO;

   // PAR.Change Time = CON.*ZERO
   PAR.Change_Time = *ZERO;

   // PAR.Change User = CON.*BLANK
   PAR.Change_User = *BLANK;

   // PAR.Change Program = CON.*BLANK
   PAR.Change_Program = *BLANK;

  }

 }

 //?Current   (new or current information)
 EXECUTE FUNCTION(Rtv Next Line NumSeq  RT) TYPE(RTVOBJ) FILE(PNGTCPP)           AC2009185;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 {
  //?USER: Processing if Data record not found

  // PAR.EDO Comment Line Seq = CON.1
  PAR.EDO_Comment_Line_Seq = 1;

  //?USER: Process Data record

  // PAR.EDO Comment Line Seq = DB1.EDO Comment Line Seq + CON.001
  PAR.EDO_Comment_Line_Seq = DB1.EDO_Comment_Line_Seq + 001;

 }

 EXECUTE FUNCTION(Crt Export Doc Order  CR) TYPE(CRTOBJ) FILE(PNGTCPP)           AC2009163;
 PARAMETER(PAR.Company_Number);
 PARAMETER(PAR.Order_Number);
 PARAMETER(PAR.ED_Export_Document_Type);
 PARAMETER(PAR.Country_Code);
 PARAMETER(PAR.EDO_Comment_Line_No);
 PARAMETER(WRK.EDO_Comment_Line_Seq);
 PARAMETER(PAR.EDO_Change_Type);
 PARAMETER(PAR.Curr_Edo_Comment_Text);
 PARAMETER(PAR.Program_Name);
 {
  //?USER: Processing before Data update

  EXECUTE FUNCTION(Set Crt Date/Time PASPGIF) TYPE(EXCINTFUN)                     AC2010529;
  PARAMETER(DB1.Record_Status);
  PARAMETER(DB1.Create_Date);
  PARAMETER(DB1.Create_Time);
  PARAMETER(DB1.Create_User);
  PARAMETER(DB1.Create_Program);
  PARAMETER(DB1.Change_Date);
  PARAMETER(DB1.Change_Time);
  PARAMETER(DB1.Change_User);
  PARAMETER(DB1.Change_Program);
  PARAMETER(PAR.Program_Name);
  {
   //?Execute internal function

   //?*** Set RECORD STATUS field. ***
   // PAR.Record Status = CND.Active
   PAR.Record_Status = 'A';

   //?*** Set CREATE fields. ***
   // PAR.Create Date = JOB.*Job date
   PAR.Create_Date = JOB.*Job_date;

   // PAR.Create Time = JOB.*Job time
   PAR.Create_Time = JOB.*Job_time;

   // PAR.Create User = JOB.*USER
   PAR.Create_User = JOB.*USER;

   //?Update the program name
   // PAR.Create Program = PAR.Program Name
   PAR.Create_Program = PAR.Program_Name;

   //?*** Set CHANGE fields. ***
   // PAR.Change Date = CON.*ZERO
   PAR.Change_Date = *ZERO;

   // PAR.Change Time = CON.*ZERO
   PAR.Change_Time = *ZERO;

   // PAR.Change User = CON.*BLANK
   PAR.Change_User = *BLANK;

   // PAR.Change Program = CON.*BLANK
   PAR.Change_Program = *BLANK;

  }

 }

}


ENDIF;

