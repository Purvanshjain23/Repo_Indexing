             PGM

             DCLF       FILE(PRKPRGL)

 LOOP:       RCVF
             MONMSG     MSGID(CPF0000) EXEC(DO)
             CHKTAP     DEV(TAP04) ENDOPT(*UNLOAD)
             RETURN
             ENDDO

             IF         COND(&OM$VOL *NE 'PRKPRG') THEN(GOTO +
                          CMDLBL(LOOP))

             RSTOBJ     OBJ(PKBVCPPPRG) SAVLIB(PRKFLIB) DEV(TAP04) +
                          OBJTYPE(*FILE) VOL(&OM$VOL) +
                          SEQNBR(&OM$SEQ) ENDOPT(*LEAVE) +
                          FILEMBR((PKBVCPPPRG)) MBROPT(*ALL) +
                          RSTLIB(PRKFHST)
             GOTO       CMDLBL(LOOP)
             ENDPGM
