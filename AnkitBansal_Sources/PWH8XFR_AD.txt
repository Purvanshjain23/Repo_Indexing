// ?------------------------------------------------------------------------------------------------
// ?Synon action diagram for PWH8XFR
// ?Date: 14.08.2025 Time: 02:56:32
// ?------------------------------------------------------------------------------------------------

//?Execute user function

//?Modifications and Comments
DO;

//?E14473 JJH 02/08/19 - Add PT SQL Format - No logic changes
//?E14393 JJH 01/18/19 - Set the FTP Method = Blank
//?- For all records in the Producer Trading Partner file.
//?- This is intended to be used in conjunction with the Refresh
//?  of the Test Environmnets.  Setting the FTP Method = Blank will
//?  not generate BrickFTP files when the Payment Post process is ran.
ENDDO;

EXECUTE FUNCTION(Rtv Set FTP Meth=BlankRT) TYPE(RTVOBJ) FILE(PLB6REP)           AB1348752;
{
 //?USER: Process Data record

 //?Set PT FTP Method = Blank
 EXECUTE FUNCTION(Chg Producer Trade PtnCH) TYPE(CHGOBJ) FILE(PLB6REP)           AB1322294;
 PARAMETER(DB1.PT_Producer_Trade_Partner);
 PARAMETER(DB1.PT_Producer_Trade_Ptnr_Nm);
 PARAMETER(*BLANK);
 PARAMETER(DB1.PT_Summary_View);
 PARAMETER(DB1.PT_Detail_View);
 PARAMETER(DB1.PT_SQL_Format);
 PARAMETER(DB1.RS_User_Changed);
 PARAMETER(DB1.RS_Date_Changed);
 PARAMETER(DB1.RS_Time_Changed);
 PARAMETER(DB1.RS_Record_Status);
 PARAMETER(DB1.RS_Job);
 PARAMETER(DB1.RS_Program);
 {
  //?USER: Processing before Data update

  EXECUTE FUNCTION(Rtv Change Stamp      IF) TYPE(EXCINTFUN)                      AB1100934;
  PARAMETER(DB1.RS_User_Changed);
  PARAMETER(DB1.RS_Date_Changed);
  PARAMETER(DB1.RS_Time_Changed);
  PARAMETER(DB1.RS_Job);
  PARAMETER(DB1.RS_Program);
  {
   //?Execute internal function

   // PAR.RS User Changed = JOB.*USER
   PAR.RS_User_Changed = JOB.*USER;

   // PAR.RS Date Changed = JOB.*Job date
   PAR.RS_Date_Changed = JOB.*Job_date;

   // PAR.RS Time Changed = JOB.*Job time
   PAR.RS_Time_Changed = JOB.*Job_time;

   // PAR.RS Job = JOB.*JOB
   PAR.RS_Job = JOB.*JOB;

   // PAR.RS Program = JOB.*PROGRAM
   PAR.RS_Program = JOB.*PROGRAM;

  }

 }

}


