// ?------------------------------------------------------------------------------------------------
// ?Synon action diagram for PBGMXFR
// ?Date: 14.08.2025 Time: 03:47:54
// ?------------------------------------------------------------------------------------------------

//?Execute user function

//?Read 360 with primal grp report and update 960 one time only
EXECUTE FUNCTION(Rtv Prd Sch fGuy to TFRT) TYPE(RTVOBJ) FILE(CABZREP)           AC2056307;
PARAMETER(360);
{
 //?USER: Processing if Data record not found

 MOVE *ALL (To: PAR From: CON);

 //?USER: Process Data record

 //?update Triumph Foods
 EXECUTE FUNCTION(Upd Item Cat06 TF Only) TYPE(RTVOBJ) FILE(CABZREP)             AC2056308;
 PARAMETER(960);
 PARAMETER(DB1.Item_Code);
 PARAMETER(DB1.Item_Category_Number_6);
 PARAMETER(DB1.Item_Primal_Sched_Report);
 {
  //?USER: Process Data record

  EXECUTE FUNCTION(Chg ItemCat06         CH) TYPE(CHGOBJ) FILE(CABZREP)           AC2048816;
  PARAMETER(DB1.Company_Number);
  PARAMETER(DB1.Item_Code);
  PARAMETER(PAR.Item_Category_Number_6);
  PARAMETER(PAR.Item_Primal_Sched_Report);
  {
   //?USER: Processing before Data update

   EXECUTE FUNCTION(Set Date/Time Stamp   IF) TYPE(EXCINTFUN)                      AC1142219;
   PARAMETER(DB1.Job_Time);
   PARAMETER(DB1.User_Id);
   PARAMETER(DB1.Job_Name);
   PARAMETER(DB1.Job_Date);
   {
    //?Execute internal function

    // PAR.Job Time = JOB.*Job time
    PAR.Job_Time = JOB.*Job_time;

    // PAR.User Id = JOB.*USER
    PAR.User_Id = JOB.*USER;

    // PAR.Job Name = JOB.*JOB
    PAR.Job_Name = JOB.*JOB;

    // PAR.Job Date = JOB.*Job date
    PAR.Job_Date = JOB.*Job_date;

   }

  }

 }

}


