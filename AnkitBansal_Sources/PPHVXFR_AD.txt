// ?------------------------------------------------------------------------------------------------
// ?Synon action diagram for PPHVXFR
// ?Date: 14.08.2025 Time: 03:40:42
// ?------------------------------------------------------------------------------------------------

//?Execute user function

EXECUTE FUNCTION(TF Fix Load Detail    RT) TYPE(RTVOBJ) FILE(OMFLCPP)           AC1713174;
PARAMETER(960);
{
 //?USER: Process Data record

 EXECUTE FUNCTION(TF Fix Load Detail    RT) TYPE(RTVOBJ) FILE(OPBFCPP)           AC1713176;
 PARAMETER(DB1.Load_ID);
 PARAMETER(LCL.Order_Number);
 PARAMETER(DB1.Stop_Drop_Sequence_Number);
 {
  //?USER: Initialize routine

  // LCL.Count USR = CON.1
  LCL.Count_USR = 1;

  //?USER: Process Data record

  CASE;

  // IF LCL.Count USR EQ PAR.Stop/Drop Sequence Number
  IF LCL.Count_USR = PAR.Stop_Drop_Sequence_Number;

  // PAR.Order Number = DB1.Order Number
  PAR.Order_Number = DB1.Order_Number;

  ENDIF;

  // LCL.Count USR = LCL.Count USR + CON.1
  LCL.Count_USR = LCL.Count_USR + 1;

 }

 EXECUTE FUNCTION(TF Fix Load Detail    CH) TYPE(CHGOBJ) FILE(OMFLCPP)           AC1713177;
 PARAMETER(DB1.Load_ID);
 PARAMETER(DB1.Stop_Drop_Sequence_Number);
 PARAMETER(LCL.Order_Number);
}


