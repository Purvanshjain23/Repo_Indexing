// ?------------------------------------------------------------------------------------------------
// ?Synon action diagram for PBYJXFR
// ?Date: 14.08.2025 Time: 03:49:45
// ?------------------------------------------------------------------------------------------------

//?Execute user function

EXECUTE FUNCTION(RTV Chg TMS Load Late RT) TYPE(RTVOBJ) FILE(PMAMREP)           AC2127697;
PARAMETER(PAR.Order_Number);
PARAMETER(PAR.Load_ID);
PARAMETER(PAR.OHE_Late_Delivery_Code);
{
 //?USER: Processing if Data record not found

 //?better be here already
 //?USER: Process Data record

 //?RMC 1/23/2020 only update it if it is blank- so dont blank it out
 //?RMC 4/12/2020 only update if par isnt blank- so dont blank it out
 CASE;

 // IF PAR.OHE Late Delivery Code is Not Blanks
 IF PAR.OHE_Late_Delivery_Code <> *BLANK;

 EXECUTE FUNCTION(CHG Existing OHE-Late  CH) TYPE(CHGOBJ) FILE(PMAMREP)          AC2127698;
 PARAMETER(DB1.Company_Number);
 PARAMETER(DB1.Order_Number);
 PARAMETER(PAR.OHE_Late_Delivery_Code);
 {
  //?USER: Processing if Data record not found

  // PGM.*Return code = CND.*Normal
  PGM.*Return_code = *BLANK;

  //?USER: Processing before Data update

  // DB1.Job Time = JOB.*Job time
  DB1.Job_Time = JOB.*Job_time;

  // DB1.Job Date = JOB.*Job date
  DB1.Job_Date = JOB.*Job_date;

  // DB1.Job Name = JOB.*PROGRAM
  DB1.Job_Name = JOB.*PROGRAM;

  // DB1.User Id = JOB.*USER
  DB1.User_Id = JOB.*USER;

 }

 ENDIF;

}


