// ?------------------------------------------------------------------------------------------------
// ?Synon action diagram for PLK1ETR
// ?Date: 14.08.2025 Time: 02:54:35
// ?------------------------------------------------------------------------------------------------

//?USER: Initialize program

// CTL.PD Name = PAR.PD Name
CTL.PD_Name = PAR.PD_Name;

// CTL.PD Producer Code = PAR.PD Producer Code
CTL.PD_Producer_Code = PAR.PD_Producer_Code;

//?USER: Create header DBF record

EXECUTE FUNCTION(Crt Producer Email HdrCR) TYPE(CRTOBJ) FILE(PLAZREP)           AB1281485;
PARAMETER(CTL.PD_Producer_Code);
PARAMETER(CTL.PEH_Email_Status);
{
 //?USER: Processing before Data update

 // DB1.PEH Email Status = CND.Yes
 DB1.PEH_Email_Status = 'Y';

 EXECUTE FUNCTION(Rtv Add Stamp-Sys Dt  IF) TYPE(EXCINTFUN)                      AB1237005;
 PARAMETER(DB1.RS_Record_Status);
 PARAMETER(DB1.RS_Job);
 PARAMETER(DB1.RS_Program);
 PARAMETER(DB1.RS_User_Added);
 PARAMETER(DB1.RS_Date_Added);
 PARAMETER(DB1.RS_Time_Added);
 {
  //?Execute internal function

  // PAR.RS Record Status = CND.Active
  PAR.RS_Record_Status = 'A';

  // PAR.RS User Added = JOB.*USER
  PAR.RS_User_Added = JOB.*USER;

  // Call program Rtv System Date & Tim UP.
  CALL PROGRAM(Rtv System Date & Tim UP) ('PKWDUPC');
  PARAMETER(PAR.RS_Date_Added);
  PARAMETER(PAR.RS_Time_Added);

  // PAR.RS Program = JOB.*PROGRAM
  PAR.RS_Program = JOB.*PROGRAM;

  // PAR.RS Job = JOB.*JOB
  PAR.RS_Job = JOB.*JOB;

 }

}


//?USER: Change header DBF record

EXECUTE FUNCTION(Chg Producer Email HdrCH) TYPE(CHGOBJ) FILE(PLAZREP)           AB1281486;
PARAMETER(CTL.PD_Producer_Code);
PARAMETER(CTL.PEH_Email_Status);
PARAMETER(CTL.RS_Record_Status);
{
 //?USER: Processing before Data update

 // DB1.PEH Email Status = CND.Yes
 DB1.PEH_Email_Status = 'Y';

 EXECUTE FUNCTION(Rtv Change Stamp-SysDtIF) TYPE(EXCINTFUN)                      AB1237006;
 PARAMETER(DB1.RS_User_Changed);
 PARAMETER(DB1.RS_Date_Changed);
 PARAMETER(DB1.RS_Time_Changed);
 PARAMETER(DB1.RS_Job);
 PARAMETER(DB1.RS_Program);
 {
  //?Execute internal function

  // PAR.RS User Changed = JOB.*USER
  PAR.RS_User_Changed = JOB.*USER;

  // Call program Rtv System Date & Tim UP.
  CALL PROGRAM(Rtv System Date & Tim UP) ('PKWDUPC');
  PARAMETER(PAR.RS_Date_Changed);
  PARAMETER(PAR.RS_Time_Changed);

  // PAR.RS Job = JOB.*JOB
  PAR.RS_Job = JOB.*JOB;

  // PAR.RS Program = JOB.*PROGRAM
  PAR.RS_Program = JOB.*PROGRAM;

 }

}


//?USER: Delete detail DBF record

EXECUTE FUNCTION(Dlt Producer EmlDtl   DO) TYPE(DLTOBJ) FILE(PLA1REP)           AB1281287;
PARAMETER(CTL.PD_Producer_Code);
PARAMETER(RCD.PED_Sequence);

//?USER: Change detail DBF record

EXECUTE FUNCTION(Chg Producer EmlDtl   CH) TYPE(CHGOBJ) FILE(PLA1REP)           AB1281286;
PARAMETER(RCD.PD_Producer_Code);
PARAMETER(RCD.PED_Sequence);
PARAMETER(RCD.PED_Email_Line);
PARAMETER(RCD.RS_Record_Status);
{
 //?USER: Processing before Data update

 EXECUTE FUNCTION(Rtv Change Stamp-SysDtIF) TYPE(EXCINTFUN)                      AB1237006;
 PARAMETER(DB1.RS_User_Changed);
 PARAMETER(DB1.RS_Date_Changed);
 PARAMETER(DB1.RS_Time_Changed);
 PARAMETER(DB1.RS_Job);
 PARAMETER(DB1.RS_Program);
 {
  //?Execute internal function

  // PAR.RS User Changed = JOB.*USER
  PAR.RS_User_Changed = JOB.*USER;

  // Call program Rtv System Date & Tim UP.
  CALL PROGRAM(Rtv System Date & Tim UP) ('PKWDUPC');
  PARAMETER(PAR.RS_Date_Changed);
  PARAMETER(PAR.RS_Time_Changed);

  // PAR.RS Job = JOB.*JOB
  PAR.RS_Job = JOB.*JOB;

  // PAR.RS Program = JOB.*PROGRAM
  PAR.RS_Program = JOB.*PROGRAM;

 }

}


//?USER: Create detail DBF record

EXECUTE FUNCTION(Crt Producer EmlDtl   IF) TYPE(EXCINTFUN) FILE(PLA1REP)        AB1281315;
PARAMETER(RCD.PD_Producer_Code);
PARAMETER(RCD.PED_Sequence);
PARAMETER(RCD.PED_Email_Line);
PARAMETER(RCD.RS_Record_Status);
{
 //?Execute internal function

 EXECUTE FUNCTION(Rtv PrdEmlDtlNxtLneNo  RT) TYPE(RTVOBJ) FILE(PLA1REP)          AB1281316;
 PARAMETER(PAR.PD_Producer_Code);
 PARAMETER(PAR.PED_Sequence);
 {
  //?USER: Processing if Data record not found

  // PAR.PED Sequence = CON.1
  PAR.PED_Sequence = 1;

  //?USER: Process Data record

  // PAR.PED Sequence = DB1.PED Sequence + CON.1
  PAR.PED_Sequence = DB1.PED_Sequence + 1;

  QUIT;

 }

 EXECUTE FUNCTION(Crt Producer EmlDtl   CR) TYPE(CRTOBJ) FILE(PLA1REP)           AB1281285;
 PARAMETER(PAR.PD_Producer_Code);
 PARAMETER(PAR.PED_Sequence);
 PARAMETER(PAR.PED_Email_Line);
 {
  //?USER: Processing before Data update

  EXECUTE FUNCTION(Rtv Add Stamp-Sys Dt  IF) TYPE(EXCINTFUN)                      AB1237005;
  PARAMETER(DB1.RS_Record_Status);
  PARAMETER(DB1.RS_Job);
  PARAMETER(DB1.RS_Program);
  PARAMETER(DB1.RS_User_Added);
  PARAMETER(DB1.RS_Date_Added);
  PARAMETER(DB1.RS_Time_Added);
  {
   //?Execute internal function

   // PAR.RS Record Status = CND.Active
   PAR.RS_Record_Status = 'A';

   // PAR.RS User Added = JOB.*USER
   PAR.RS_User_Added = JOB.*USER;

   // Call program Rtv System Date & Tim UP.
   CALL PROGRAM(Rtv System Date & Tim UP) ('PKWDUPC');
   PARAMETER(PAR.RS_Date_Added);
   PARAMETER(PAR.RS_Time_Added);

   // PAR.RS Program = JOB.*PROGRAM
   PAR.RS_Program = JOB.*PROGRAM;

   // PAR.RS Job = JOB.*JOB
   PAR.RS_Job = JOB.*JOB;

  }

 }

}


