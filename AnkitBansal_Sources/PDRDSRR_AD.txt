// ?------------------------------------------------------------------------------------------------
// ?Synon action diagram for PDRDSRR
// ?Date: 14.08.2025 Time: 03:29:30
// ?------------------------------------------------------------------------------------------------

//?USER: Load subfile record from DBF record

EXECUTE FUNCTION(Rtv Prod Dept Info    RT) TYPE(RTVOBJ) FILE(PDI5REP)           AC1336752;
PARAMETER(CTL.Company_Number);
PARAMETER(CTL.Department_Code);
PARAMETER(WRK.Department_Description);
PARAMETER(WRK.Department_Type);
{
 //?USER: Processing if Data record not found

 MOVE *ALL (To: PAR From: CON);

 //?USER: Process Data record

 MOVE *ALL (To: PAR From: DB1);

}


EXECUTE FUNCTION(Rtv Mix Priority      RT) TYPE(RTVOBJ) FILE(PDI6CPP)           AC1454573;
PARAMETER(CTL.Company_Number);
PARAMETER(CTL.Department_Code);
PARAMETER(1);
PARAMETER(RCD.Item_Code);
PARAMETER(RCD.Mix_Priority);
{
 //?USER: Processing if Data record not found

 MOVE *ALL (To: PAR From: CON);

 //?USER: Process Data record

 MOVE *ALL (To: PAR From: DB1);

 QUIT;

}


CASE;

// IF WRK.Department Type is Credit
IF WRK.Department_Type = 'C';

CASE;

// IF DB1.Item Type Code is Finished Goods
IF DB1.Item_Type_Code = 'FG';

// PGM.*Record selected = CND.*YES
PGM.*Record_selected = 'Y';

// IF *OTHERWISE
IF *OTHERWISE;

// PGM.*Record selected = CND.*NO
PGM.*Record_selected = 'N';

ENDIF;

// IF *OTHERWISE
IF *OTHERWISE;

CASE;

// IF DB1.Item Type Code is Finished Goods
IF DB1.Item_Type_Code = 'FG';

// AND DB1.Credit Item Status is no
AND DB1.Credit_Item_Status = 'N';

// AND RCD.Converted/Base Item is Base
AND RCD.Base_Conv_Loss_Item = 'B';

// PGM.*Record selected = CND.*YES
PGM.*Record_selected = 'Y';

// IF *OTHERWISE
IF *OTHERWISE;

// PGM.*Record selected = CND.*NO
PGM.*Record_selected = 'N';

ENDIF;

ENDIF;

